# SQLite Refactor Log

This log is the durable record for the clean-slate AsyncStorage-to-SQLite refactor.

## Current Phase Checklist

- [x] Spec signoff
- [x] Plan 1 - Backend and config guardrails
- [x] Plan 2 - Foundation and schema
- [x] Plan 3 - Repositories and storage facade
- [x] Plan 4 - Sync engine and outbox
- [ ] Plan 5 - Context and screen migration
- [ ] Plan 6 - Cleanup, export, and release gate

## Decision Register

| Date | Decision | Rationale | Revisit Trigger |
| --- | --- | --- | --- |
| 2026-05-20 | Use a clean-slate SQLite cutover instead of migrating local AsyncStorage domain data. | Field users can install a fresh app and restart from the new backend, which removes the riskiest compatibility work. | User decides current field-test device data must be preserved. |
| 2026-05-20 | Split the old monolithic plan into one spec plus six smaller implementation plans. | Zazi's refactor found many late review issues; smaller reviewable plans reduce conflict and make logs more useful. | A plan grows across unrelated ownership boundaries. |
| 2026-05-20 | Keep npm for this refactor. | The repo currently uses npm and package-lock; changing package managers during storage replacement adds avoidable risk. | User explicitly asks for pnpm migration or dependency security work becomes the main task. |
| 2026-05-20 | Separate job titles from programmes. | Job titles are profile/HR labels; programmes are operational dimensions for groups, sessions, assessments, and child work. | A staff member can hold multiple active programme assignments at the same time. |
| 2026-05-20 | Use `child_ea_assignments` and `child_group_memberships` in the clean schema. | The new backend is not constrained by old table names, and the EA wording matches the operational handoff model better than generic staff wording. | Masi decides non-EA staff roles must use the same table name. |
| 2026-05-20 | Migrate `app.json` to `app.config.js` in Plan 1. | Supabase target guardrails need dynamic config; treating this as optional leaves the old EAS `.env` pitfall in place. | Expo config needs to stay static for an EAS constraint we uncover. |
| 2026-05-20 | Auto-enroll newly created children into the actor's current programme. | Child creation should produce a complete working record without requiring a separate programme enrollment step. | EAs are allowed to create children for a programme other than their active assignment. |
| 2026-05-20 | Allow children to have multiple concurrent programme enrollments. | The same child may receive literacy, numeracy, and other support from different EAs; programme belongs to the work relationship, not permanently to the child. | Masi decides a child can belong to only one programme at a time. |
| 2026-05-20 | Scope My Children to the user's active programme. | EAs work one programme at a time, so the working list should show children assigned to the user and enrolled in that current programme. | Product needs a default cross-programme child list. |
| 2026-05-20 | Permit broad cross-programme reads for the same child at RLS level and enforce programme display in repositories. | Trusted field staff may need handover/history visibility, and simpler RLS is easier to debug than programme-isolated policies. | Masi adds less-trusted partner, parent, or external read-only users. |
| 2026-05-20 | Use dual SELECT policies for mobile-created tables. | Assignment/membership SELECT handles normal visibility; `created_by = auth.uid()` preserves upsert visibility before related join rows sync. | Backend no longer uses Supabase upserts from the mobile client. |
| 2026-05-20 | Enforce handover write blocking through active `child_ea_assignments`. | Historical assignees can read prior work, but old EAs should not keep writing child-specific event rows after handover. | Product wants old EAs to keep contributing after handover. |
| 2026-05-20 | Keep `time_entries` self-scoped by `user_id`. | Time tracking belongs to the staff member, not to child assignment state. | Time entries become manager-editable through the mobile app. |
| 2026-05-20 | Use one-shot field cutover with user-managed 1-2 day notice. | The user accepts losing unsynced device data at cutover and prefers a clean simultaneous restart. | Field operations require a rolling or reversible cutover. |
| 2026-05-20 | Keep `AGENTS.md` and `CLAUDE.md` aligned with the clean-slate refactor before implementation starts. | Future agents should not optimize for legacy backwards compatibility or skip the refactor log/TDD discipline after the user locked the clean-slate decision. | The refactor finishes or the user changes the cutover strategy. |
| 2026-05-21 | Treat `masi-app-sqlite` as the clean-slate backend intended to become primary after validation. | The user created the new Supabase project and left current `masi-app` intact, matching the clean-slate refactor plan. | User decides to keep `masi-app-sqlite` staging-only and create a separate production backend later. |
| 2026-05-21 | Add `academic_years` as a first-class table; classes become year-specific via `academic_year_id`. | User confirmed multi-year operation; without academic years, year-over-year reporting collides on class name. | Masi pivots to single-year operation. |
| 2026-05-21 | Use a hard-fail invariant for multiple active academic years. | The partial unique index keeps year activation deliberate and simple; the active 2026 seed supplies the required current year. | Admin workflow needs one migration to activate a new year and deactivate the old one atomically. |
| 2026-05-21 | Add `assessment_windows` with baseline/midline/endline; tie official assessments to a window and leave ad-hoc progress checks windowless. | Formal assessment campaigns are part of Masi reporting; date-range inference is too brittle. | Masi drops formal assessment windows. |
| 2026-05-21 | Add `child_class_memberships` with academic year; `children.class_id` becomes a current/compatibility pointer. | Children can change classes mid-year, and old work must stay linked to the old class for reports. | Masi decides children never change classes mid-year. |
| 2026-05-21 | Add `teachers` table without teacher assignment history tables. | Reporting on teachers is not planned now but might come later; a first-class current teacher table is cheap to add now and expensive to retrofit. | Reporting needs teacher-level history; add `teacher_school_assignments` and `class_teacher_assignments` then. |
| 2026-05-21 | Add `class_ea_assignments` and `group_ea_assignments`; EAs can be assigned at child, class, or group level. | User confirmed Masi uses a mix of all three assignment levels in practice. | Masi simplifies to a single assignment level. |
| 2026-05-21 | Add `grouping_versions` and `class_grouping_state` for versioned grouping. | EAs regroup mid-year and prior groupings must remain as history. | Masi decides regrouping is rare enough to do in place without versioning. |
| 2026-05-21 | Skip Zazi tables `education_assistants`, `child_school_enrollments`, `staff_identity_links`, external ID bridges, `teacher_school_assignments`, and `class_teacher_assignments`. | Each can be added later if the operational model needs it; carrying them now adds RLS surface without clear value. | Admin/PM features need a separate EA roster, child mobility between schools becomes common, or teacher history becomes reportable. |
| 2026-05-21 | Write policy for child-specific event rows uses an OR across direct child assignment, class assignment plus active class membership, and group assignment plus active group membership. | EAs can earn write access through any of the three assignment paths; all paths must include both active assignment and active membership filters to prevent stale-assignment writes. | A new assignment path is added, or the multi-path OR becomes too expensive per row. |
| 2026-05-21 | Disable direct mobile hard delete on `children`; use `delete_child_if_no_history(child_id)` for the rare no-history delete path. | Child event rows have cascading FKs, so direct delete could silently erase assessment, mastery, and attendance history. Children with history should be archived or unassigned instead. | Product introduces a server-side admin console with explicit audited delete approval. |
| 2026-05-21 | Archive repositories must end active assignments/enrollments in the same transaction as the archive marker. | Archived children/classes/groups should not stay active in My Children, class, group, or assignment reports. | Archive semantics change to mean display-only hidden state. |
| 2026-05-21 | Admin-precreated classes are supported; EAs may self-assign only when the class is visible through their active school/programme assignment. | Masi will have admin-created/prepopulated classes; requiring `classes.created_by = auth.uid()` would block legitimate mobile claiming. | Admin workflows move all class assignment through service-role only. |
| 2026-05-21 | Future active-year rollover is a deliberate migration transaction, not automatic year switching. | The active-year unique index hard-fails multiple active years; future maintainers need to deactivate the old year and activate the new year atomically. | Masi wants an admin UI to change active years interactively. |
| 2026-05-21 | Make the SQLite staging launcher responsible for local Android SDK discovery and a fixed Metro port. | Codex can use the user's Android emulator when the project launcher injects `ANDROID_HOME`, `ANDROID_SDK_ROOT`, and SDK tool paths; pinning the staging Metro port avoids non-interactive Expo prompts when another Metro server already owns 8081. | The repo moves to a managed dev-client workflow with a different launch path. |
| 2026-05-21 | Use normalized SQLite repositories as the Plan 3 domain boundary and keep `src/utils/storage.js` as a temporary facade. | Repositories now own SQL, transaction boundaries, outbox enqueue, and normalized table contracts while current contexts/screens continue using named storage methods. | Plan 5 finishes migrating contexts/screens directly to repositories and the facade can be removed. |
| 2026-05-21 | Preserve current screen-shaped payloads temporarily in `local_state` sidecars while SQLite stores normalized rows. | Existing screens still expect AsyncStorage-style objects such as session `children_ids` and assessment letter arrays; the sidecar prevents a UI rewrite inside Plan 3 while normalized rows become the source for sync. | Plan 5 removes remaining screen-level storage callers and reads normalized repository view models directly. |
| 2026-05-21 | Treat child creation as four domain writes when class is known: child, EA assignment, programme enrollment, and class membership. | The schema has four durable rows for that action, so Plan 3 enqueues four matching outbox items rather than inventing a fifth side-effect row. | A later sync contract adds an explicit audit/event row for child creation. |
| 2026-05-21 | Relationship rows created by child save use fresh UUIDs plus active-row lookups for idempotency. | Deterministic child/user/programme/class IDs erase handover history when an ended relationship later becomes active again; active unique indexes already enforce single active rows. | A future sync model requires deterministic client-generated IDs for conflict-free relationship creation. |
| 2026-05-21 | `deleteClass` and `deleteGroup` are archive aliases in the storage facade layer. | Mobile hard delete without a synced outbox signal creates server orphans; archive preserves history and keeps a syncable mutation. | Product explicitly adds no-history hard-delete flows for classes or groups. |
| 2026-05-21 | Local-only legacy session metadata stays in SQLite but is stripped from session outbox payloads. | Current screens need legacy fields until Plan 5, but Supabase should not receive `__legacySession` implementation details as opaque session activity data. | Plan 5 removes legacy session fields from the repository shape entirely. |
| 2026-05-21 | Treat `sync_outbox` as the only Plan 4 push source. | Scanning domain rows by `sync_status` repeats AsyncStorage-era failure modes; repositories already enqueue durable row-level operations. | Plan 4 code review finds a required sync operation that cannot be represented as an outbox row. |
| 2026-05-21 | Delete `repairOrphanedJunctions` instead of porting it. | It repaired a historical AsyncStorage/junction bug and is not applicable to the clean-slate normalized SQLite schema. | Field telemetry from the SQLite build proves a new orphan pattern that needs a different repair. |
| 2026-05-21 | Treat `23505` duplicate-key responses as success only for explicitly configured tables. | Global duplicate-as-success can hide genuine partial-unique conflicts; success must be an intentional table-level decision. | A future server RPC makes duplicate handling idempotent for all mobile writes. |
| 2026-05-21 | Make local legacy programme references terminal sync failures. | The temporary `local-legacy-programme` row is deliberately terminal/local-only; records that depend on it should stay visible as unsyncable instead of trying to push invalid foreign keys. | Plan 5 removes the storage facade paths that can create legacy programme fallback records. |
| 2026-05-21 | Build Supabase payloads from table-specific server-column allowlists. | Sidecar compatibility fields such as `children_ids`, `group_ids`, and EGRA summary arrays must not leak from local view models into normalized Supabase tables. | Server schema intentionally adds a new mobile-writable column; add it to the allowlist and tests. |
| 2026-05-21 | Split `OfflineContext` into non-blocking `triggerBackgroundSync()` and shared-promise `syncNow()`. | Write paths need to refresh local status immediately without waiting for upload, while manual sync callers should wait for the same active sync instead of launching duplicates. | Product needs a blocking write-and-sync flow for a specific workflow. |
| 2026-05-21 | Time entry repository writes must enqueue `sync_outbox` rows in the same SQLite transaction as the domain row. | Clock-in/out is a first-class offline write; relying on later `sync_status` scans would recreate the AsyncStorage-era gap Plan 4 is removing. | Time entries move to a server RPC that owns both domain mutation and queueing. |
| 2026-05-21 | `syncAll()` resets stale `in_flight` outbox rows to `pending` at the start of each cycle. | A mobile process can die after marking a row in flight; restart must retry instead of stranding the row forever. | The engine gains a durable lease/heartbeat model with expiry windows. |
| 2026-05-21 | Outbox finalization uses compare-and-swap on `sync_outbox.id`, `updated_at`, and `status = 'in_flight'`. | If a user edits a row while an older payload is uploading, the older finalize must not delete the newer outbox payload or mark the domain row synced. | Sync processing becomes single-threaded across process lifetime by a stronger platform lock. |
| 2026-05-21 | Production duplicate-key handling remains terminal by default. | With `upsert(..., onConflict: 'id')`, `23505` usually means a different unique constraint was violated and should stay visible for review. | A table-specific duplicate can be proven safe and added to `safeDuplicateSuccessTables`. |
| 2026-05-21 | Plan 4 keeps `pullReferenceData` as an exported full-replace helper; production wiring is deferred to Plan 5. | Plan 4 built and tested the cache refresh primitive, but context hydration belongs with the Plan 5 screen/context migration. | First authenticated SQLite sign-in needs reference caches before Plan 5 starts. |
| 2026-05-21 | Domain outbox enqueue resets retry metadata whenever a user write refreshes the payload. | A user correction should retry immediately and should not inherit an old `next_retry_at` or stale error from the payload it replaced. | The sync engine moves retry state to immutable attempt rows instead of the active outbox row. |
| 2026-05-21 | Unknown sync tables terminalize the outbox row without updating a domain table. | Malformed or future-table rows should be visible failed items, not stuck pending rows or crashes from updating a table that does not exist locally. | Dynamic table registration is added to the sync engine. |
| 2026-05-21 | Auth profile/preload reads are deferred out of `onAuthStateChange` and serialized through a Supabase request queue. | Supabase warns against awaiting database calls inside auth callbacks; serial preload requests also reduce cold-start request races. | Supabase client behavior changes or a dedicated backend bootstrap RPC replaces client-side profile reads. |
| 2026-05-21 | ChildrenContext hydrates children, groups, and child-group memberships through one cache-first preload path. | Zazi hit duplicate pull races when providers performed separate server pulls; one bundle distributes results to all setters and preserves cache on partial errors. | Plan 5 introduces a richer domain pull engine that owns children/classes/groups hydration. |
| 2026-05-21 | Class creation uses the active `academic_years` row automatically. | Classes are year-specific in the SQLite schema; relying on screens to pass the year would create inconsistent class rows. | Masi allows users to create future-year classes from a picker. |
| 2026-05-21 | Class archive/delete contexts delegate child side effects to repository/storage transactions. | Context-level child nulling double-wrote child rows and could fight repository archive semantics. | Repository archive semantics are removed and class deletion becomes a purely visual filter. |
| 2026-05-21 | Context server pulls save rows through typed repository-backed storage methods instead of generic `storage.setItem(STORAGE_KEYS...)` cache replacement. | Generic list replacement bypasses normalized repository behavior and can recreate the AsyncStorage-era cache wipe pattern Plan 5 is removing. | A future pull engine owns full domain replacement atomically across related tables. |
| 2026-05-21 | Keep the remaining direct `AsyncStorage` usage centralized in `src/utils/storage.js` until Plan 6. | Plan 5 removed screen/context/component generic storage callers; removing the whole storage facade/profile fallback in the same slice would enlarge the auth startup blast radius. | Plan 6 starts cleanup/export/release gate work. |
| 2026-05-21 | Delete `asyncStorageSanitizer` and `SchemaHardeningBootstrap` instead of porting them. | They existed to repair legacy AsyncStorage data shapes; clean-slate SQLite installs do not need a compatibility sanitizer running at startup. | Product decides to support in-place upgrades from legacy AsyncStorage builds. |
| 2026-05-21 | Default user-facing repository reads are programme-scoped when called with `userId`. | My Children, sessions, assessments, letter mastery, groups, classes, stats, and rankings should show the actor's current programme slice by default. Cross-programme history remains an explicit future view, not the standard field workflow. | Product adds a cross-programme history/admin toggle. |
| 2026-05-21 | User-facing domain writes fail fast when no active programme assignment exists. | Clean-slate builds should not create `local-legacy-programme` rows from sessions, assessments, mastery, or groups; a missing assignment is a setup/data problem that should remain visible. | Product needs offline draft creation before programme assignment is available. |
| 2026-05-21 | UI child creation uses the atomic repository path. | Add Child must create child, EA assignment, programme enrollment, and class membership in one local transaction instead of saving a child row plus a legacy staff-child row. | Child creation moves to a backend RPC. |
| 2026-05-21 | Align Masi's Supabase client to Zazi's `@supabase/supabase-js@2.100.1` during Plan 5 Android validation. | The older 2.91.0 auth lock implementation can emit delayed lock-timeout warnings because it does not clear successful acquire timers; Zazi is already on the patched client family. | Supabase auth client changes regress React Native process-lock behavior. |
| 2026-05-21 | Use one singleton Supabase client/AppState listener across module reloads. | Expo/Fast Refresh and Android restarts should not create duplicate clients, refresh tickers, or AppState listeners against the same persisted auth storage key. | App moves to a dev-client/runtime where module reload behavior is proven different. |
| 2026-05-21 | Auth startup relies on `onAuthStateChange`'s `INITIAL_SESSION` event instead of also calling `getSession()`. | `getSession()` plus `INITIAL_SESSION` creates duplicate startup lock contenders; one official initial-session path is enough and is easier to reason about. | Supabase removes or changes the initial-session subscription behavior. |

## Bug And Gap Register

| Date | Gap/Bug | Resolution | Reuse Note |
| --- | --- | --- | --- |
| 2026-05-20 | Original plan over-weighted backwards compatibility. | Superseded with clean-slate spec and six smaller plans. | Ask whether field data must be preserved before designing storage migrations. |
| 2026-05-20 | Zazi review findings were present as lessons but not mandatory contracts. | Added transaction, outbox finalization, pull concurrency, PRAGMA, RLS, and real-device validation contracts to the sub-plans. | Every Zazi production trap should become a testable contract. |
| 2026-05-20 | `AGENTS.md` still emphasized backwards-compatible production schema changes without calling out the SQLite refactor exception. | Updated `AGENTS.md` and `CLAUDE.md` to point to the clean-slate spec, TDD skill, npm exception, app.config.js migration, and mandatory refactor log. | Keep agent context files aligned when major project direction changes. |
| 2026-05-21 | Shell-sourcing `.env.local` failed because `SUPABASE_DB_PASSWORD_SQLITE` contains shell metacharacters. | Plan 1 helper must parse `.env` / `.env.local` as dotenv-style data and must not execute the file with `.` or `source`. | Any future script reads Supabase DB passwords from env files. |
| 2026-05-21 | Fresh `masi-app-sqlite` advisors report `public.rls_auto_enable()` as a SECURITY DEFINER function executable by `anon` and `authenticated`. | Resolved in `20260521120147_masi_rls_advisor_cleanup.sql` by revoking execute from `public`, `anon`, and `authenticated`. | A new Supabase project ships with exposed helper functions. |
| 2026-05-21 | After the first Plan 1 migrations were applied, advisors still reported `rls_auto_enable()` execute exposure and many `auth_rls_initplan` policy warnings. | Resolved with a dedicated RLS advisor cleanup migration that uses `(select auth.uid())` in policy predicates. Dual SELECT `multiple_permissive_policies` warnings on `children`, `classes`, and `groups` remain intentional because they preserve mobile upsert visibility. | Any future RLS migration uses `(select auth.uid())` and explicitly handles security-definer function execute privileges. |
| 2026-05-21 | `sqlite:staging:push` prompted for confirmation during the cleanup migration push. | Added `--yes` to the helper's `db-push` action so future migration pushes are non-interactive. | Any CLI helper command is used in a release gate or CI-like flow. |
| 2026-05-21 | Self-review found `staff_programme_assignments` did not enforce the one-active-programme-per-EA assumption. | Add a partial unique index on `staff_programme_assignments(user_id)` where `ended_at is null`. | Product allows one EA to actively work multiple programmes at the same time. |
| 2026-05-21 | Plan 1 review found recursive RLS between `sessions` SELECT and `session_attendees` SELECT, producing `42P17 infinite recursion detected`. | Add non-recursive private helper functions and replace the session-related SELECT policies. | Any RLS policy on one table queries another table whose policy queries back into the first table. |
| 2026-05-21 | Plan 1 review found staging helper guardrails could target the wrong Supabase project if `_SQLITE` env vars are misconfigured. | Require the known `masi-app-sqlite` project ref and exact Supabase hostname in staging validation. | New SQLite backend project ref changes intentionally. |
| 2026-05-21 | Plan 1 review found session parent write policies did not enforce the handover contract after attendees exist. | Tighten session update/delete policies to require an active assignment for at least one session attendee; keep insert parent programme-scoped because attendees are inserted after the parent row and enforce child assignment. | Session creation moves to a backend RPC that can validate parent and attendees atomically. |
| 2026-05-21 | Plan 1 review found `assessments` and `letter_mastery` UPDATE checks did not re-check the new child/programme state. | Mirror insert-style active child assignment and active programme checks in `WITH CHECK`. | Repository-level immutability is later proven sufficient and RLS can be simplified safely. |
| 2026-05-21 | Follow-up Plan 1 review found `child_ea_assignments` could grant child access too broadly and attendee update/delete did not require parent session ownership. | Add a corrective migration that limits mobile assignment insert to children the actor created, removes mobile assignment update/delete policies, and routes attendee update/delete through a private ownership helper. | Masi adds a server-controlled handover RPC or admin mobile flow. |
| 2026-05-21 | Follow-up Plan 1 review found `sqlite:staging:link` passed the DB password as a process argument. | Move link to `supabase --yes link --project-ref ...` and rely on `SUPABASE_DB_PASSWORD` in the command environment if supported by the CLI. | Supabase CLI requires explicit `--password`; document the process-arg limitation if fallback is needed. |
| 2026-05-21 | Initial Masi clean-slate schema was leaner than Zazi by eight tables. | Add a Zazi-alignment migration with academic years, assessment windows, teachers, child-class history, class/group EA assignments, grouping versions, class grouping state, column additions, and an updated RLS helper. | Compare new clean-slate schemas against the most-similar existing app before treating a spec as final. |
| 2026-05-21 | Seed migration did not include schools, an active academic year, or a baseline assessment window. | Extend Plan 1 with Zazi-alignment seed data: active 2026 year, 2026 baseline window, and 325 schools from `scripts/masi-schools-db-apr26.csv`. | Treat "first sign-in shows useful reference data" as a Plan 1 acceptance criterion. |
| 2026-05-21 | Review found direct child hard delete would cascade and erase child history. | Add a child delete guard migration that drops the child delete policy, revokes direct delete, and exposes only `delete_child_if_no_history(child_id)` for no-history deletes. | Any new table references `children(id)` with `on delete cascade`; update the RPC's history check before using hard delete. |
| 2026-05-21 | `npx expo install expo-sqlite` cannot automatically update dynamic `app.config.js`. | Manually added the `expo-sqlite` config plugin entry after dependency install. | Any future Expo install expects automatic config mutation after the Plan 1 static-to-dynamic config migration. |
| 2026-05-21 | The first Plan 2 transaction serialization test used bare promise flushing, which made the assertion sensitive to JavaScript scheduling rather than behavior. | Replaced the flush with an explicit deferred signal from the mocked native transaction boundary. | Async ordering tests assert on a concrete observable signal. |
| 2026-05-21 | Plan 2 Android staging launch initially could not complete in the current shell. | Sandbox run failed before Metro with `ERR_SOCKET_BAD_PORT`; rerun outside sandbox started Metro, then failed because no Android device/emulator was visible from Expo. Later investigation found the Android SDK installed under `~/Library/Android/sdk`; the staging launcher now adds the SDK tools to the command environment and uses port 8082. | Any future local device gate depends on tools installed outside the default shell `PATH`. |
| 2026-05-21 | Plan 2 review found local schema drift from the backend and sync contract. | Added active unique indexes for `child_ea_assignments` and `child_programme_enrollments`, changed row `sync_status` to `pending/synced/failed/terminal`, changed outbox operations to `insert/update/archive/hard_delete/restore`, and allowed terminal outbox rows. | Any Plan 4 sync-engine contract changes the outbox operation/status vocabulary. |
| 2026-05-21 | Plan 2 review found concurrent `runMigrations()` calls bypassed the app-level write queue. | Added a migration-level promise queue so a second migration run re-reads `PRAGMA user_version` only after the first run commits and updates the pragma. | Migration runner is moved into a single boot-time call site that is already serialized and tests prove that invariant. |
| 2026-05-21 | Plan 3 full-suite verification exposed flaky SQLite foundation assertions when the file ran after UI/storage tests. | Scope migration serialization per explicit database handle and replace the constraint helper's `expect(promise).rejects.toThrow(...)` path with explicit native-error capture. | Any new real-SQLite test relies on native `better-sqlite3` constraint errors inside the Jest Expo environment. |
| 2026-05-21 | Plan 3 caller scan found existing generic `storage.setItem` and `STORAGE_KEYS` use in contexts, screens, and the AsyncStorage sanitizer. | Route those calls through the temporary SQLite-backed storage facade for now and save the grep output as a Plan 5 migration input. | Plan 5 starts context/screen migration; do not infer callsites from filenames. |
| 2026-05-21 | Plan 3 review found deterministic child relationship IDs erased handover/rejoin history. | Generate new UUIDs for new relationship rows and use active-row lookups to make repeated saves idempotent without overwriting ended rows. | Any relationship repository derives IDs from natural keys. |
| 2026-05-21 | Plan 3 review found `archiveChild` left active `child_group_memberships` rows. | Archive now ends active group memberships and enqueues archive outbox rows alongside EA, programme, and class relationships. | Any new child relationship table is added to archive and no-history checks together. |
| 2026-05-21 | Plan 3 review found `deleteClass` and `deleteGroup` were local-only hard deletes. | Both methods now route to archive, preserving rows locally and producing syncable archive outbox entries. | A future hard-delete-if-no-history product requirement is added. |
| 2026-05-21 | Plan 3 review found synced server rows could be re-enqueued by repository save helpers. | Added `shouldEnqueueOutbox` and skip outbox rows for `sync_status = 'synced'` or `terminal`; `syncStatusFromSynced(undefined)` now defaults to `pending` for local writes. | Plan 4 changes row status vocabulary or import paths for server pulls. |
| 2026-05-21 | Plan 4 TDD exposed that legacy session view-model fields could still reach Supabase from outbox payloads. | Added table-specific server-column allowlists and tests for sessions and assessments so `children_ids`, `group_ids`, `session_type_id`, `correct_letters`, and related local fields are stripped. | Any new repository enqueues a payload built from a screen-shaped object instead of normalized columns. |
| 2026-05-21 | Plan 4 TDD exposed that records using `local-legacy-programme` would be attempted against Supabase. | `offlineSync` now marks those rows terminal with a visible failed item instead of calling Supabase. | Plan 5 removes storage-facade fallback paths or active programme assignment is guaranteed before writes. |
| 2026-05-21 | Plan 4 review found `timeEntriesRepository` wrote domain rows without enqueueing outbox rows. | Added repository producer tests and same-transaction outbox enqueue for time-entry create/update, with synced server rows still suppressed. | Every repository write contract needs a producer-to-engine test, not only a repository-local outbox assertion. |
| 2026-05-21 | Plan 4 review found `in_flight` rows could be stranded forever after app restart. | Added restart-recovery coverage and `syncOutboxRepository.resetInFlight()`, called at the start of every `syncAll()` cycle. | Any status that excludes a row from readiness needs a restart recovery path. |
| 2026-05-21 | Plan 4 review found sync finalization could delete a newer local outbox payload created while the old payload was in flight. | Added a concurrent-edit test and compare-and-swap finalization; stale finalizers restore/leave the row pending and keep the domain row pending. | Multi-step state transitions need a stale-writer test that mutates storage while the mocked server call is in progress. |
| 2026-05-21 | Plan 4 review found null outbox payloads could be converted to `{ id }` and sent as empty upserts. | Non-delete records with missing payload now fail terminal locally with no Supabase call. | Outbox producers should treat payload absence as a data integrity bug, not as an implicit minimal payload. |
| 2026-05-21 | Plan 4 review agent found unknown-table rows could never terminalize after CAS finalization was added. | Added an outbox-only terminal finalizer and regression test so unknown tables become visible failed items without touching a domain table. | When finalization logic grows CAS predicates, malformed-row paths that never enter `in_flight` need separate coverage. |
| 2026-05-21 | Plan 4 review agent found user writes could inherit stale retry backoff metadata. | `insertOutboxRecord` now resets `retry_count`, `last_error`, and `next_retry_at` on enqueue conflict; time-entry update coverage proves fresh user edits retry immediately. | Any durable retry metadata colocated with the active outbox row must be cleared when the payload changes. |
| 2026-05-21 | Plan 5 auth review found profile fetches could complete after sign-out and repopulate stale state. | Added profile-load versioning and sign-out invalidation so stale local/server profile results are ignored. | Any context starts async work that can outlive the current authenticated user. |
| 2026-05-21 | Plan 5 context review found ChildrenContext still had separate mount pulls for children, groups, and memberships. | Added a one-call preloaded child data service and cache-preserving bundle hydration in ChildrenContext. | Any provider hydrates related domains through independent server calls. |
| 2026-05-21 | Plan 5 Task 4 found ChildrenContext and ClassesContext still used generic `storage.setItem`/`STORAGE_KEYS` list replacement for server pull cache writes. | Persist pulled rows through typed methods (`saveChild`, `saveGroup`, `saveChildrenGroup`, `saveClass`) so repository contracts remain the write boundary. | Any context merges server rows directly into local cache state. |
| 2026-05-21 | Clean-slate Plan 5 still had the legacy AsyncStorage schema sanitizer and bootstrap test. | Deleted `asyncStorageSanitizer`, `SchemaHardeningBootstrap`, and sanitizer state facade methods; fresh SQLite installs should rely on schema migrations and repository normalization instead. | User reintroduces in-place local data upgrades from legacy app builds. |
| 2026-05-21 | Android staging bundle failed after sanitizer deletion because `App.js` still imported `SchemaHardeningBootstrap`. | Removed the stale root import/wrapper and added an App-root smoke test that fails when deleted bootstrap imports remain. | Any cleanup deletes a provider/bootstrap component that is imported only from the root app entry. |
| 2026-05-21 | Plan 5 review found default reads could leak cross-programme sessions, assessments, mastery, groups, and classes. | Repository read APIs now accept `{ userId }`, resolve the actor's active programme, and user-facing screens/contexts pass the current user. No active programme returns an empty read set. | Any screen adds a repository read without passing `userId`; add a grep/review check. |
| 2026-05-21 | Plan 5 review found letter mastery reuse could mutate a row from another programme. | `persistLiteracySession`, `LetterTrackerScreen`, and `LetterTrackerBottomSheet` now scope mastery/assessment reads by active programme and child; regression tests prove cross-programme deleted mastery rows are not reused. | Any natural-key lookup ignores `programme_id`. |
| 2026-05-21 | Plan 5 review found child delete still used legacy `hidden_at` and class archive left active child class memberships. | `storage.deleteChild` now tries no-history hard delete, otherwise archives; class archive ends child-class memberships, nulls current child class pointers, and enqueues the child/member updates. | Any archive path changes relationship tables or display filters. |
| 2026-05-21 | Plan 5 review found removed group memberships could reappear in active lists. | `groupsRepository.getChildrenGroups()` excludes removed memberships by default and ChildrenContext filters removed memberships in group helpers and duplicate checks. | A history view needs removed memberships; call `includeRemoved: true` explicitly. |
| 2026-05-21 | Plan 5 review found Auth/Children/Classes context state could survive sign-out or stale async work. | Auth invalidates in-flight profile loads on manual sign-out; Children and Classes contexts clear state when user disappears and guard async state updates by active user id. | Any provider starts async hydration keyed to auth user id. |
| 2026-05-21 | Android Plan 5 validation surfaced repeated Supabase auth lock warnings during signed-in startup. | Aligned Supabase JS/Auth to 2.100.1, made the Supabase client a singleton, removed duplicate startup `getSession()`, and queued remaining startup reference-data reads. Fresh Android logcat stayed quiet for 35 seconds after restart. | Treat auth-lock warnings as device-validation findings, not harmless test noise, until verified on Android. |

## Plan 5 Screen-Level Storage Caller Enumeration

Source command:

```bash
rg "storage\\." src/screens src/hooks src/components
```

Output captured before Task 3 screen edits:

```text
src/screens/insights/SessionCountRankingScreen.js:        const sessions = await storage.getSessions();
src/screens/insights/LetterMasteryRankingScreen.js:          storage.getAssessments(),
src/screens/insights/LetterMasteryRankingScreen.js:          storage.getLetterMastery(),
src/hooks/useTimeTracking.js:    await storage.updateTimeEntry(entry.id, updatedEntry);
src/hooks/useTimeTracking.js:      const entries = await storage.getTimeEntries();
src/hooks/useTimeTracking.js:      await storage.saveTimeEntry(timeEntry);
src/hooks/useTimeTracking.js:      await storage.updateTimeEntry(activeEntry.id, updatedEntry);
src/components/session/LetterTrackerBottomSheet.js:        const allAssessments = await storage.getAssessments();
src/components/session/LetterTrackerBottomSheet.js:        const allMastery = await storage.getLetterMastery();
src/components/session/LetterTrackerBottomSheet.js:  const allAssessments = await storage.getAssessments();
src/components/session/LetterTrackerBottomSheet.js:  const allMastery = await storage.getLetterMastery();
src/screens/main/ChildrenListScreen.js:        const assessments = await storage.getAssessments();
src/screens/assessments/ChildAssessmentSummaryScreen.js:        const all = await storage.getAssessments();
src/screens/assessments/AssessmentChildSelectScreen.js:        const allAssessments = await storage.getAssessments();
src/screens/insights/AssessmentRankingScreen.js:        const assessments = await storage.getAssessments();
src/screens/assessments/AssessmentHistoryScreen.js:      const cached = await storage.getAssessments();
src/screens/assessments/AssessmentHistoryScreen.js:          await storage.setItem(STORAGE_KEYS.ASSESSMENTS, merged);
src/screens/main/AssessmentsScreen.js:        const assessments = await storage.getAssessments();
src/screens/assessments/LetterAssessmentScreen.js:    await storage.saveAssessment(assessment);
src/screens/main/TimeEntriesListScreen.js:      const cached = await storage.getTimeEntries();
src/screens/main/TimeEntriesListScreen.js:          await storage.setItem(STORAGE_KEYS.TIME_ENTRIES, merged);
src/screens/main/SessionsScreen.js:        const sessions = await storage.getSessions();
src/screens/assessments/LetterTrackerScreen.js:      const allAssessments = await storage.getAssessments();
src/screens/assessments/LetterTrackerScreen.js:      const allMastery = await storage.getLetterMastery();
src/screens/assessments/LetterTrackerScreen.js:      await storage.updateLetterMasteryRecord(recordId, {
src/screens/assessments/LetterTrackerScreen.js:      const allMastery = await storage.getLetterMastery();
src/screens/assessments/LetterTrackerScreen.js:        await storage.updateLetterMasteryRecord(existing.id, {
src/screens/assessments/LetterTrackerScreen.js:        await storage.saveLetterMasteryRecord(record);
src/screens/sessions/SessionHistoryScreen.js:      const cached = await storage.getSessions();
src/screens/sessions/SessionHistoryScreen.js:          await storage.setItem(STORAGE_KEYS.SESSIONS, merged);
src/screens/main/HomeScreen.js:          storage.getTimeEntries(),
src/screens/main/HomeScreen.js:          storage.getSessions(),
src/screens/main/HomeScreen.js:          storage.getAssessments(),
src/screens/sessions/LiteracySessionForm.js:      await storage.saveSession(session);
src/screens/sessions/LiteracySessionForm.js:            const allMastery = await storage.getLetterMastery();
src/screens/sessions/LiteracySessionForm.js:              await storage.updateLetterMasteryRecord(existingDeleted.id, {
src/screens/sessions/LiteracySessionForm.js:              await storage.saveLetterMasteryRecord(record);
src/screens/sessions/LiteracySessionForm.js:            const allMastery = await storage.getLetterMastery();
src/screens/sessions/LiteracySessionForm.js:              await storage.updateLetterMasteryRecord(existing.id, {
```

## Verification Register

| Date | Command/Check | Result | Notes |
| --- | --- | --- | --- |
| 2026-05-20 | `rg -n "clean-slate|sqlite-refactor-log|Test Driven Development|backwards-compatible|app.config.js|TDD skill|/Users/jimmckeown/Development/masi-app/.agents/skills/tdd" AGENTS.md CLAUDE.md` | Passed | Confirmed both agent context files mention the refactor, log, TDD skill, compatibility exception, and `app.config.js` direction. |
| 2026-05-20 | `rg -n "[[:blank:]]$" AGENTS.md CLAUDE.md docs/superpowers/specs/2026-05-20-sqlite-migration-design.md docs/superpowers/plans/2026-05-20-sqlite-*.md documentation/sqlite-refactor-log.md` | Passed | No trailing whitespace in updated docs. |
| 2026-05-21 | `supabase --version` | Passed | CLI version `2.95.4`. |
| 2026-05-21 | `supabase projects list -o json` | Passed | CLI authenticated; existing `masi-app` and new `masi-app-sqlite` are visible. New project ref is `segygjzpujphwvrubusm` and status is `ACTIVE_HEALTHY`. |
| 2026-05-21 | `supabase link --project-ref segygjzpujphwvrubusm --password ... --yes` | Passed | Repo is linked to the new `masi-app-sqlite` project for Plan 1. Secret was read from `.env.local` and not printed. |
| 2026-05-21 | `cat supabase/.temp/project-ref` | Passed | Linked project ref is `segygjzpujphwvrubusm`; `supabase/.temp/` is gitignored. |
| 2026-05-21 | `supabase db query --linked "select current_database() as database_name, current_user as database_user" -o json` | Passed | Query reached the linked remote database and returned `postgres` / `postgres`. |
| 2026-05-21 | `SUPABASE_DB_PASSWORD=... supabase migration list --linked` | Passed | New project has no local or remote migrations yet. |
| 2026-05-21 | `SUPABASE_DB_PASSWORD=... supabase db push --linked --dry-run` | Passed | Remote database is up to date; dry run did not attempt to push changes. |
| 2026-05-21 | `SUPABASE_DB_PASSWORD=... supabase db advisors --linked --level warn` | Warning | Reported `public.rls_auto_enable()` SECURITY DEFINER function executable by `anon` and `authenticated`; captured as a Plan 1 security item. |
| 2026-05-21 | `npm test -- --runInBand __tests__/supabaseProjectConfig.test.js` | Passed | TDD slice for `config/supabaseProjectConfig.js` and `app.config.js`; first run failed on missing modules, then passed after resolver/config implementation. |
| 2026-05-21 | `npm test -- --runInBand __tests__/supabaseClient.test.js __tests__/supabaseProjectConfig.test.js` | Passed | Supabase client now refuses mismatched target/project URL combinations before creating a client. |
| 2026-05-21 | `npm test -- --runInBand __tests__/sqlitePlan1Migrations.test.js` | Failed as expected | Red TDD check: no `rls_advisor_cleanup` migration exists yet to revoke PUBLIC execute or convert policy calls to `(select auth.uid())`. |
| 2026-05-21 | `npm test -- --runInBand __tests__/sqliteStagingScript.test.js` | Failed as expected | Red TDD check: `db-push` did not include `--yes`, so the helper could prompt interactively. |
| 2026-05-21 | `npm test -- --runInBand __tests__/sqlitePlan1Migrations.test.js` | Passed | Cleanup migration now revokes PUBLIC execute and every `auth.uid()` in that migration is wrapped as `(select auth.uid())`. |
| 2026-05-21 | `npm run sqlite:staging:dry-run` | Passed | Would push only `20260521120147_masi_rls_advisor_cleanup.sql`. First sandboxed attempt failed with DNS resolution, then passed with network access. |
| 2026-05-21 | `npm run sqlite:staging:push` | Passed | Applied `20260521120147_masi_rls_advisor_cleanup.sql` to `masi-app-sqlite`. This run revealed the interactive prompt captured in the bug register. |
| 2026-05-21 | `npm test -- --runInBand __tests__/sqliteStagingScript.test.js` | Passed | Staging helper now includes `--yes` for `db-push` and still redacts secrets. |
| 2026-05-21 | `npm run sqlite:staging:migrations` | Passed | Local and remote migration history match: `20260521115412`, `20260521115416`, `20260521115421`, `20260521120147`. |
| 2026-05-21 | `npm run sqlite:staging:advisors` | Expected warnings only | Remaining warnings are `multiple_permissive_policies` for `public.children`, `public.classes`, and `public.groups` SELECT policies. These are intentional dual SELECT policies for assignment/programme visibility plus `created_by` upsert fallback. |
| 2026-05-21 | `npm test -- --runInBand __tests__/supabaseProjectConfig.test.js __tests__/supabaseClient.test.js __tests__/sqliteStagingScript.test.js __tests__/sqlitePlan1Migrations.test.js` | Passed | Plan 1 targeted test set passed 4 suites / 13 tests. |
| 2026-05-21 | `npm run sqlite:staging:check` | Passed | SQLite staging env loads from `.env.local`, validates project ref/URL, and redacts DB password and publishable key. |
| 2026-05-21 | `rg -n "replace\\(|pg_policies|staff_children|children_groups|children_ids|correct_letters|incorrect_letters" supabase/migrations` | Passed | No forbidden policy string replacement or legacy table/JSON field names in canonical Supabase migrations. |
| 2026-05-21 | `npm test -- --runInBand` | Passed | Full Jest suite passed 19 suites / 81 tests. Existing React Native Paper icon warnings remain non-blocking test noise. |
| 2026-05-21 | `npm run sqlite:staging:dry-run` | Passed | Remote database is up to date after the cleanup migration. |
| 2026-05-21 | `npm test -- --runInBand __tests__/sqlitePlan1Migrations.test.js` | Failed as expected | Red TDD check: no partial unique index enforced one active `staff_programme_assignments` row per user. |
| 2026-05-21 | `npm test -- --runInBand __tests__/sqlitePlan1Migrations.test.js` | Passed | Added `20260521135520_masi_staff_programme_assignment_uniqueness.sql` with a partial unique index on active staff programme assignments. |
| 2026-05-21 | `npm run sqlite:staging:dry-run` | Passed | Would push only `20260521135520_masi_staff_programme_assignment_uniqueness.sql`. |
| 2026-05-21 | `npm run sqlite:staging:push` | Passed | Applied `20260521135520_masi_staff_programme_assignment_uniqueness.sql` to `masi-app-sqlite`. |
| 2026-05-21 | `npm run sqlite:staging:migrations` | Passed | Local and remote migration history now match through `20260521135520`. |
| 2026-05-21 | `npm run sqlite:staging:advisors` | Expected warnings only | Still only the intentional `multiple_permissive_policies` warnings for `children`, `classes`, and `groups`. |
| 2026-05-21 | `npm run sqlite:staging:dry-run` | Passed | Remote database is up to date after the staff assignment uniqueness migration. |
| 2026-05-21 | Plan 1 parallel review checkpoint | Failed | Review found RLS recursion, staging-project guardrail weakness, session handover write gaps, incomplete update `WITH CHECK` predicates, and substring URL matching. |
| 2026-05-21 | `npm test -- --runInBand __tests__/supabaseProjectConfig.test.js __tests__/sqliteStagingScript.test.js __tests__/sqlitePlan1Migrations.test.js` | Failed as expected | Red TDD check for review findings: URL substring guard accepted malformed hosts, staging helper accepted primary project env, and no `rls_review_fixes` migration existed yet. |
| 2026-05-21 | `npm test -- --runInBand __tests__/supabaseProjectConfig.test.js __tests__/sqliteStagingScript.test.js __tests__/sqlitePlan1Migrations.test.js` | Passed | Exact Supabase hostname validation, known SQLite project-ref validation, private RLS helpers, session update/delete handover checks, and assessment/letter mastery update `WITH CHECK` contracts are now covered. |
| 2026-05-21 | `npm test -- --runInBand __tests__/sqliteStagingScript.test.js` | Failed as expected | Red TDD check: Supabase CLI still prompted when `--yes` was placed after `db push`; move it to the global flag position. |
| 2026-05-21 | `npm test -- --runInBand __tests__/sqliteStagingScript.test.js` | Passed | `db-push` now calls Supabase as `supabase --yes db push --linked`. |
| 2026-05-21 | `npm run sqlite:staging:dry-run` | Passed | Would push only `20260521140331_masi_rls_review_fixes.sql`. |
| 2026-05-21 | `npm run sqlite:staging:push` | Passed | Applied `20260521140331_masi_rls_review_fixes.sql` to `masi-app-sqlite`. This run still printed the old prompt because it ran before the helper was changed to put `--yes` in the global flag position. |
| 2026-05-21 | `npm run sqlite:staging:migrations` | Passed | Local and remote migration history now match through `20260521140331`. |
| 2026-05-21 | `npm run sqlite:staging:advisors` | Expected warnings only | Still only the intentional `multiple_permissive_policies` warnings for `children`, `classes`, and `groups`; no security-definer or `auth_rls_initplan` warnings returned. |
| 2026-05-21 | Rollback-only RLS recursion probe through `supabase db query --linked` | Passed | As authenticated user `00000000-0000-0000-0000-000000000101`, session and attendee SELECTs each returned 1 row with no `42P17` recursion. Transaction rolled back. |
| 2026-05-21 | Rollback-only RLS handover update probe through `supabase db query --linked` | Probe harness failed first, then passed | First attempt could not insert into a temp results table after switching to `authenticated`; rerun granted temp-table access. Active assignment updated 1 session row; after `child_ea_assignments.unassigned_at`, the same authenticated update returned 0 rows. Transaction rolled back. |
| 2026-05-21 | `npm run sqlite:staging:check` | Passed | Stricter staging helper accepts only `segygjzpujphwvrubusm` and redacts secrets. |
| 2026-05-21 | `npx expo config --type public` | Passed | Dynamic `app.config.js` resolves the default primary target and preserves app identity, runtime policy, update URL, and EAS project ID. |
| 2026-05-21 | `npm test -- --runInBand __tests__/supabaseProjectConfig.test.js __tests__/supabaseClient.test.js __tests__/sqliteStagingScript.test.js __tests__/sqlitePlan1Migrations.test.js` | Passed | Targeted Plan 1 test set passed 4 suites / 18 tests after review fixes. |
| 2026-05-21 | `git diff --check` | Passed | No whitespace errors in the Plan 1 diff. |
| 2026-05-21 | `rg -n "replace\\(|pg_policies|staff_children|children_groups|children_ids|correct_letters|incorrect_letters" supabase/migrations` | Passed | No forbidden RLS string replacement and no legacy storage table/JSON names in canonical migrations. |
| 2026-05-21 | `npm test -- --runInBand` | Passed | Full Jest suite passed 19 suites / 86 tests. Existing React Native Paper icon warnings remain non-blocking test noise. |
| 2026-05-21 | Follow-up Plan 1 parallel review checkpoint | Failed | Review found assignment-table access escalation, attendee update/delete ownership gap, and DB password process-argument exposure in `sqlite:staging:link`. |
| 2026-05-21 | `npm test -- --runInBand __tests__/sqlitePlan1Migrations.test.js __tests__/sqliteStagingScript.test.js` | Failed as expected | Red TDD check for follow-up review findings: no `assignment_attendee_fixes` migration and link still included DB password in argv. |
| 2026-05-21 | `npm test -- --runInBand __tests__/sqlitePlan1Migrations.test.js __tests__/sqliteStagingScript.test.js` | Passed | Follow-up review fixes are now covered by migration and staging-helper tests. |
| 2026-05-21 | `npm run sqlite:staging:dry-run` | Passed | Would push only `20260521142324_masi_assignment_attendee_fixes.sql`. |
| 2026-05-21 | `npm run sqlite:staging:push` | Passed | Applied `20260521142324_masi_assignment_attendee_fixes.sql` to `masi-app-sqlite`. Supabase CLI still prints the confirmation prompt even with `--yes`, but proceeds without manual secret exposure. |
| 2026-05-21 | `npm run sqlite:staging:link` | Passed | Updated link helper works without passing the DB password in process args; password is supplied through `SUPABASE_DB_PASSWORD` env. |
| 2026-05-21 | `npm run sqlite:staging:migrations` | Passed | Local and remote migration history now match through `20260521142324`. |
| 2026-05-21 | `npm run sqlite:staging:advisors` | Expected warnings only | Still only the intentional `multiple_permissive_policies` warnings for `children`, `classes`, and `groups`. |
| 2026-05-21 | Rollback-only assignment escalation probe through `supabase db query --linked` | Probe harness failed | Shell expanded PL/pgSQL `$$` delimiters into a process ID, causing SQL syntax error before the RLS check ran. Rerun with escaped delimiters. |
| 2026-05-21 | Rollback-only assignment escalation probe through `supabase db query --linked` | Failed | Both own-child and other-child assignment inserts returned `42P17`, because the assignment INSERT policy queried `children`, whose SELECT policy queries `child_ea_assignments`. Add a private helper to bypass RLS recursion for this policy check. |
| 2026-05-21 | `npm test -- --runInBand __tests__/sqlitePlan1Migrations.test.js` | Failed as expected | Red TDD check: no `assignment_insert_recursion_fix` migration exists yet. |
| 2026-05-21 | `npm test -- --runInBand __tests__/sqlitePlan1Migrations.test.js` | Passed | Added `20260521143346_masi_assignment_insert_recursion_fix.sql` with private helper-backed assignment insert policy. |
| 2026-05-21 | `npm run sqlite:staging:dry-run` | Passed | Would push only `20260521143346_masi_assignment_insert_recursion_fix.sql`. |
| 2026-05-21 | `npm run sqlite:staging:push` | Passed | Applied `20260521143346_masi_assignment_insert_recursion_fix.sql` to `masi-app-sqlite`; Supabase CLI still displays its confirmation prompt while auto-answering yes. |
| 2026-05-21 | Rollback-only assignment escalation probe through `supabase db query --linked` | Passed | As authenticated User B, assignment to User B's own created child inserted; assignment to User A's child returned `42501`. Transaction rolled back. |
| 2026-05-21 | Rollback-only cross-EA attendee update probe through `supabase db query --linked` | Passed | Other active EA on the same child updated 0 attendee rows; session owner EA updated 1 attendee row. Transaction rolled back. |
| 2026-05-21 | `npm run sqlite:staging:migrations` | Passed | Local and remote migration history now match through `20260521143346`. |
| 2026-05-21 | `npm run sqlite:staging:advisors` | Expected warnings only | Still only the intentional `multiple_permissive_policies` warnings for `children`, `classes`, and `groups`. |
| 2026-05-21 | `npm run sqlite:staging:dry-run` | Passed | Remote database is up to date after all Plan 1 corrective migrations. |
| 2026-05-21 | `npm test -- --runInBand __tests__/supabaseProjectConfig.test.js __tests__/supabaseClient.test.js __tests__/sqliteStagingScript.test.js __tests__/sqlitePlan1Migrations.test.js` | Passed | Final targeted Plan 1 suite passed 4 suites / 21 tests. |
| 2026-05-21 | `npm run sqlite:staging:check` | Passed | Final staging env guard check accepts only `segygjzpujphwvrubusm` and redacts secrets. |
| 2026-05-21 | `git diff --check` | Passed | No whitespace errors in the final Plan 1 diff. |
| 2026-05-21 | `rg -n "replace\\(|pg_policies|staff_children|children_groups|children_ids|correct_letters|incorrect_letters" supabase/migrations` | Passed | No forbidden RLS string replacement and no legacy storage table/JSON names in canonical migrations. |
| 2026-05-21 | `npm test -- --runInBand` | Passed | Final full Jest suite passed 19 suites / 89 tests. Existing React Native Paper icon warnings remain non-blocking test noise. |
| 2026-05-21 | `npm test -- --runInBand __tests__/sqlitePlan1Migrations.test.js` | Failed as expected, then passed | Red TDD check failed because no Zazi-alignment migration existed; after adding `20260521144901_masi_zazi_alignment_schema.sql`, the migration contract suite passed 12 tests. |
| 2026-05-21 | `rg -n "replace\\(|pg_policies|staff_children|children_groups|children_ids|correct_letters|incorrect_letters|education_assistants|child_school_enrollments|staff_identity_links|teacher_school_assignments|class_teacher_assignments" supabase/migrations` | Passed | No forbidden policy string replacement, legacy storage names, or explicitly skipped Zazi tables in canonical migrations. |
| 2026-05-21 | `npm run sqlite:staging:dry-run` | Passed | Would push only `20260521144901_masi_zazi_alignment_schema.sql`. |
| 2026-05-21 | `npm run sqlite:staging:push` | Passed after sandbox retry | First sandboxed attempt failed DNS resolution; rerun with network access applied `20260521144901_masi_zazi_alignment_schema.sql` to `masi-app-sqlite`. Supabase CLI still printed the confirmation prompt while auto-answering yes. |
| 2026-05-21 | `npm run sqlite:staging:migrations` | Passed | Local and remote migration history now match through `20260521144901`. |
| 2026-05-21 | `npm run sqlite:staging:advisors` | Expected warnings only | Still only the intentional `multiple_permissive_policies` warnings for `children`, `classes`, and `groups`. |
| 2026-05-21 | `supabase db query --linked` reference seed probe | Passed | Remote `masi-app-sqlite` has one active 2026 academic year, one 2026 baseline assessment window, and 325 schools. Transaction-free read only. |
| 2026-05-21 | Rollback-only active-year invariant probe through `supabase db query --linked` | Passed | Inserting a second active academic year raised `unique_violation`; transaction rolled back. |
| 2026-05-21 | Rollback-only assessment window/purpose probe through `supabase db query --linked` | Passed | Inserting an `official_window` assessment with `assessment_window_id = null` raised `check_violation`; transaction rolled back. |
| 2026-05-21 | Rollback-only child write helper probe through `supabase db query --linked` | Passed | `private.current_user_can_write_for_child` returned true via direct child, class assignment, and group assignment paths, and false for no path, ended class membership, and ended group membership. Transaction rolled back. |
| 2026-05-21 | Rollback-only grouping-version uniqueness probe through `supabase db query --linked` | Passed | Inserting a second active `grouping_versions` row for one class/year raised `unique_violation`; transaction rolled back. |
| 2026-05-21 | Rollback-only baseline trigger probe through `supabase db query --linked` | Passed | Temporarily deactivating 2026 and inserting active 2098 created one baseline assessment window; transaction rolled back. |
| 2026-05-21 | `npm test -- --runInBand __tests__/supabaseProjectConfig.test.js __tests__/supabaseClient.test.js __tests__/sqliteStagingScript.test.js __tests__/sqlitePlan1Migrations.test.js` | Passed | Targeted Plan 1 suite passed 4 suites / 26 tests after Zazi-alignment amendments. |
| 2026-05-21 | `npm run sqlite:staging:dry-run` | Passed | Remote database is up to date after applying the Zazi-alignment migration. |
| 2026-05-21 | `git diff --check` | Passed | No whitespace errors after migration, spec, plan, and log updates. |
| 2026-05-21 | `npm test -- --runInBand` | Passed | Full Jest suite passed 19 suites / 94 tests. Existing React Native Paper icon warnings remain non-blocking test noise. |
| 2026-05-21 | `npm run sqlite:staging:check` | Passed | SQLite staging env still validates the `masi-app-sqlite` project ref/URL and redacts secrets. |
| 2026-05-21 | `npm test -- --runInBand __tests__/sqlitePlan1Migrations.test.js` | Failed as expected, then passed | Red TDD check failed because no child delete guard migration existed; after adding `20260521153217_masi_child_delete_guard.sql`, the migration contract suite passed 15 tests. |
| 2026-05-21 | `npm run sqlite:staging:dry-run` | Passed | Would push only `20260521153217_masi_child_delete_guard.sql`. |
| 2026-05-21 | `npm run sqlite:staging:push` | Passed after sandbox retry | First sandboxed attempt failed DNS resolution; rerun with network access applied the child delete guard migration to `masi-app-sqlite`. |
| 2026-05-21 | `npm run sqlite:staging:migrations` | Passed | Local and remote migration history now match through `20260521153217`. |
| 2026-05-21 | `npm run sqlite:staging:advisors` | Expected warnings only | Still only the intentional `multiple_permissive_policies` warnings for `children`, `classes`, and `groups`. |
| 2026-05-21 | Rollback-only child delete guard probe through `supabase db query --linked` | Passed | Direct authenticated child delete failed with `insufficient_privilege`; `delete_child_if_no_history` deleted a no-history child, returned false for a child with assessment history, and mobile self-assignment to an admin-created class succeeded. Transaction rolled back. |
| 2026-05-21 | `npm test -- --runInBand __tests__/supabaseProjectConfig.test.js __tests__/supabaseClient.test.js __tests__/sqliteStagingScript.test.js __tests__/sqlitePlan1Migrations.test.js` | Passed | Targeted Plan 1 suite passed 4 suites / 29 tests after child delete guard. |
| 2026-05-21 | `npm run sqlite:staging:dry-run` | Passed | Remote database is up to date after applying the child delete guard migration. |
| 2026-05-21 | `git diff --check` | Passed | No whitespace errors after child delete guard, archive-contract, admin-class, and active-year docs updates. |
| 2026-05-21 | `npm test -- --runInBand` | Passed | Full Jest suite passed 19 suites / 97 tests. Existing React Native Paper icon warnings remain non-blocking test noise. |
| 2026-05-21 | `npx expo install expo-sqlite` | Passed with manual config follow-up | Installed `expo-sqlite@~16.0.10`; Expo CLI reported it could not write to dynamic `app.config.js`, so the plugin entry was added manually. Initial sandboxed attempt failed DNS resolution and was rerun with network approval. |
| 2026-05-21 | `npm install --save-dev better-sqlite3 dotenv` | Passed | Added `better-sqlite3@^12.10.0` for real-SQLite integration tests and `dotenv@^17.4.2` for env-file parsing in local test/support scripts. Initial sandboxed attempt failed DNS resolution and was rerun with network approval. |
| 2026-05-21 | `npm test -- --runInBand` | Passed | Plan 2 dependency baseline stayed green: full Jest suite passed 19 suites / 97 tests after SQLite packages and config plugin were added. Existing React Native Paper icon warnings remain non-blocking test noise. |
| 2026-05-21 | `npm test -- --runInBand __tests__/sqliteFoundation.test.js` | Failed as expected, then passed | Red failed on missing `src/db/client.js`; after adding the SQLite client and Expo SQLite mock boundary, the transaction serialization test passed. |
| 2026-05-21 | `npm test -- --runInBand __tests__/sqliteFoundation.test.js` | Passed | Plan 2 foundation suite passed 8 tests covering transaction serialization, real-SQLite migrations, `PRAGMA user_version`, 25 server-backed tables, local sync metadata, Zazi-alignment columns/invariants, rollback, and debug dump output. |
| 2026-05-21 | `git diff --check` | Passed | No whitespace errors in the Plan 2 diff. |
| 2026-05-21 | `npm test -- --runInBand` | Passed | Full Jest suite passed 20 suites / 105 tests. Existing React Native Paper icon warnings remain non-blocking test noise. |
| 2026-05-21 | `npm run sqlite:staging:android` | Blocked | Sandbox run failed with `ERR_SOCKET_BAD_PORT` before Metro. Rerun outside sandbox started Metro, then failed with `No Android connected device found, and no emulators could be started automatically.` Expo also printed package-version compatibility warnings for existing Expo packages. |
| 2026-05-21 | Plan 2 parallel review checkpoint | Failed, then fixed | Review found missing local active uniqueness, sync enum drift, async `debugDump` catch bug, and unqueued concurrent migrations. Added tests and fixes for each issue. |
| 2026-05-21 | `npm test -- --runInBand __tests__/sqliteFoundation.test.js` | Passed | Plan 2 foundation suite now passes 12 tests after review fixes, including active uniqueness, sync/outbox enum alignment, concurrent migration serialization, and a more sensitive foreign-key PRAGMA test. |
| 2026-05-21 | `git diff --check` | Passed | No whitespace errors after Plan 2 review fixes. |
| 2026-05-21 | `npm test -- --runInBand` | Passed | Full Jest suite passed 20 suites / 109 tests after Plan 2 review fixes. Existing React Native Paper icon warnings remain non-blocking test noise. |
| 2026-05-21 | `npm run sqlite:staging:ios` (visual gate) | Passed | iPhone 17 simulator booted the Masi sign-in screen against `masi-app-sqlite` staging with no SQLite redbox. Pre-existing Expo package-version warnings (`expo@54.0.32` vs `~54.0.34`, `expo-file-system`, `expo-updates`, `react-native-get-random-values`) printed but did not block the launch. |
| 2026-05-21 | Plan 2 reviewer signoff | Granted | Independent code review confirmed plan-to-code mapping for all four tasks plus the four review fixes (active uniqueness, sync enum vocabulary, async `debugDump` catch, migration-queue serialization); `npm test` rerun 12/12 in foundation suite and 109/109 across the full suite. |
| 2026-05-21 | `npm test -- --runInBand __tests__/sqliteStagingScript.test.js` | Passed | Staging helper tests now cover Android SDK path injection and fixed port 8082 for SQLite staging Expo launches. |
| 2026-05-21 | `npm run sqlite:staging:android` (visual gate) | Passed after launcher fixes | `Medium_Phone_API_35` booted, Expo installed the recommended Expo Go version, Metro bundled Android `index.js`, and the app reached the unauthenticated state against `masi-app-sqlite` with no SQLite initialization redbox. Supabase auth lock warnings appeared while unauthenticated; they are noted for later auth/storage work but did not block launch. |
| 2026-05-21 | `npm test -- --runInBand __tests__/sqliteStagingScript.test.js __tests__/sqliteFoundation.test.js` and `npm test -- --runInBand` | Passed | Targeted SQLite launcher/foundation suites passed 20 tests; full Jest suite passed 20 suites / 111 tests after Android launcher fixes and Plan 2 log updates. Existing React Native Paper icon warnings remain non-blocking test noise. |
| 2026-05-21 | `npm test -- --runInBand __tests__/sqliteRepositoryUtils.test.js __tests__/localStateRepository.test.js __tests__/referenceDataRepository.test.js __tests__/timeEntriesRepository.test.js __tests__/classesRepository.test.js __tests__/childrenRepository.test.js __tests__/groupsRepository.test.js __tests__/sessionsRepository.test.js __tests__/assessmentsRepository.test.js __tests__/masteryRepository.test.js __tests__/storage-classes.test.js __tests__/ClassesContext.test.js __tests__/ChildrenContext.hiddenChildren.test.js __tests__/offlineSync.pendingSessions.test.js __tests__/asyncStorageSanitizer.test.js` | Passed | Plan 3 targeted repository/storage facade suite passed 15 suites / 54 tests. Existing offline sync skip logging remains expected test noise. |
| 2026-05-21 | `rg "storage\\.(getItem|setItem|removeItem)|STORAGE_KEYS" src __tests__ --glob '!src/utils/storage.js'` | Passed with known follow-up callers | Existing callers remain in `ClassesContext`, `ChildrenContext`, `TimeEntriesListScreen`, `SessionHistoryScreen`, `AssessmentHistoryScreen`, `asyncStorageSanitizer`, and related tests; these are Plan 5 migration inputs. |
| 2026-05-21 | `npm test -- --runInBand` | Passed | Plan 3 full Jest suite passed 30 suites / 133 tests after repository facade wiring, per-database migration queue isolation, and explicit native SQLite error capture in the foundation helper. Existing React Native Paper icon warnings remain non-blocking test noise. |
| 2026-05-21 | `git diff --check` | Passed | No whitespace errors in the Plan 3 repository/storage facade diff. |
| 2026-05-21 | `npm test -- --runInBand __tests__/childrenRepository.test.js __tests__/classesRepository.test.js __tests__/groupsRepository.test.js` | Failed as expected, then passed | Red tests reproduced the Plan 3 review findings for relationship history overwrite, missing group-membership archive, and class/group local-only delete; fixes made all 14 tests pass. |
| 2026-05-21 | `npm test -- --runInBand __tests__/sqliteRepositoryUtils.test.js __tests__/childrenRepository.test.js __tests__/sessionsRepository.test.js` | Passed | Covered sync-status default semantics, synced child save outbox suppression, archived-class My Children filtering, legacy session outbox stripping, and terminal legacy programme fallback. |
| 2026-05-21 | `npm test -- --runInBand __tests__/storage-classes.test.js __tests__/ClassesContext.test.js __tests__/ChildrenContext.hiddenChildren.test.js __tests__/offlineSync.pendingSessions.test.js __tests__/asyncStorageSanitizer.test.js` | Passed | Storage facade compatibility stayed green after class/group delete became archive and active list queries started filtering archived rows. |
| 2026-05-21 | `npm test -- --runInBand` | Passed | Full Jest suite passed 30 suites / 142 tests after Plan 3 review fixes. Existing React Native Paper icon warnings remain non-blocking test noise. |
| 2026-05-21 | `git diff --check` | Passed | No whitespace errors after Plan 3 review fixes. |
| 2026-05-21 | `npm test -- --runInBand __tests__/syncOutboxRepository.test.js` | Failed as expected, then passed | Red failed on missing sync outbox/state repositories; green covers idempotent enqueue, persisted retries, retry readiness, failed/terminal visibility, in-flight count exclusion, and sync state metadata. |
| 2026-05-21 | `npm test -- --runInBand __tests__/offlineSyncOutbox.test.js` | Failed as expected, then passed | Red failed on missing outbox sync engine; green covers push order, parent failure skip, duplicate-key safety configuration, terminal `23503`/`42501`, retry scheduling without sleeps, child hard-delete RPC, and reference cache pull order. |
| 2026-05-21 | `npm test -- --runInBand __tests__/OfflineContext.test.js` | Failed as expected, then passed | Red failed because `OfflineContext` still imported obsolete orphan repair and lacked the Plan 4 API; green covers debounced `triggerBackgroundSync`, shared-promise `syncNow`, and non-blocking status refresh. |
| 2026-05-21 | `npm test -- --runInBand __tests__/syncOutboxRepository.test.js __tests__/offlineSyncOutbox.test.js __tests__/OfflineContext.test.js` | Passed | Plan 4 targeted suite passed 3 suites / 14 tests. |
| 2026-05-21 | `npm test -- --runInBand __tests__/offlineSync.stripping.test.js __tests__/offlineSync.pendingSessions.test.js __tests__/offlineSyncOutbox.test.js` | Passed | Payload allowlist and local-legacy-programme terminal handling passed alongside outbox sync behavior. |
| 2026-05-21 | `npm test -- --runInBand __tests__/syncOutboxRepository.test.js __tests__/offlineSyncOutbox.test.js __tests__/OfflineContext.test.js __tests__/offlineSync.stripping.test.js __tests__/offlineSync.pendingSessions.test.js __tests__/storage-classes.test.js __tests__/ClassesContext.test.js __tests__/ChildrenContext.hiddenChildren.test.js __tests__/asyncStorageSanitizer.test.js` | Passed | Broader Plan 4 sync/storage compatibility set passed 9 suites / 49 tests. Existing OfflineContext sync logs remain expected test noise. |
| 2026-05-21 | `npm test -- --runInBand` | Passed | Full Jest suite passed 33 suites / 156 tests after Plan 4 outbox sync implementation. Existing React Native Paper icon warnings and OfflineContext sync logs remain non-blocking test noise. |
| 2026-05-21 | `git diff --check` | Passed | No whitespace errors after Plan 4 sync engine, context, tests, plan, and log updates. |
| 2026-05-21 | `npm test -- --runInBand __tests__/timeEntriesRepository.test.js` | Failed as expected, then passed | Red tests caught missing time-entry outbox producer rows; green covers create/update enqueue and synced server-row suppression. |
| 2026-05-21 | `npm test -- --runInBand __tests__/offlineSyncOutbox.test.js` | Failed as expected, then passed | Red tests caught stranded `in_flight`, stale success finalization, and null-payload upsert fallback; green now covers those plus archive/restore upsert behavior. |
| 2026-05-21 | `npm test -- --runInBand __tests__/timeEntriesRepository.test.js __tests__/syncOutboxRepository.test.js __tests__/offlineSyncOutbox.test.js __tests__/OfflineContext.test.js __tests__/offlineSync.stripping.test.js __tests__/offlineSync.pendingSessions.test.js` | Passed | Corrective Plan 4 targeted set passed 6 suites / 30 tests after review fixes. Existing OfflineContext sync logs remain expected test noise. |
| 2026-05-21 | `npm test -- --runInBand` | Passed | Full Jest suite passed 33 suites / 164 tests after Plan 4 review fixes. Existing React Native Paper icon warnings and OfflineContext sync logs remain non-blocking test noise. |
| 2026-05-21 | `rg -n "enqueueDomainOutbox|resetInFlight|updated_at = \\?|MISSING_OUTBOX_PAYLOAD|safeDuplicateSuccessTables|setPullState" src/db/repositories/timeEntriesRepository.js src/db/repositories/syncOutboxRepository.js src/db/repositories/syncStateRepository.js src/services/offlineSync.js` | Passed | Confirmed time-entry outbox enqueue, in-flight reset, CAS predicates, missing-payload terminal classification, duplicate-key default comment, and Plan 5 pull-state note are present. |
| 2026-05-21 | `git diff --check` | Passed | No whitespace errors after Plan 4 review fixes. |
| 2026-05-21 | Plan 4 corrective parallel review checkpoint | Failed, then fixed | One reviewer found no blockers; the correctness reviewer found unknown-table terminalization and stale retry-backoff gaps. Added red tests and central fixes for both. |
| 2026-05-21 | `npm test -- --runInBand __tests__/timeEntriesRepository.test.js __tests__/offlineSyncOutbox.test.js` | Failed as expected, then passed | Red tests reproduced the review-agent findings: unknown tables attempted a domain update, and refreshed time-entry payloads kept old retry metadata. Green passed 2 suites / 19 tests. |
| 2026-05-21 | `npm test -- --runInBand __tests__/timeEntriesRepository.test.js __tests__/syncOutboxRepository.test.js __tests__/offlineSyncOutbox.test.js __tests__/OfflineContext.test.js __tests__/offlineSync.stripping.test.js __tests__/offlineSync.pendingSessions.test.js` | Passed | Follow-up corrective Plan 4 targeted set passed 6 suites / 32 tests after retry metadata reset and unknown-table terminalization fixes. Existing OfflineContext sync logs remain expected test noise. |
| 2026-05-21 | `npm test -- --runInBand` | Passed | Full Jest suite passed 33 suites / 166 tests after follow-up Plan 4 review-agent fixes. Existing React Native Paper icon warnings and OfflineContext sync logs remain non-blocking test noise. |
| 2026-05-21 | `rg -n "retry_count|last_error|next_retry_at|finalizeOutboxOnlyTerminalFailure|Unknown sync table|resetInFlight|MISSING_OUTBOX_PAYLOAD" src/db/repositories/sqliteRepositoryUtils.js src/services/offlineSync.js __tests__/timeEntriesRepository.test.js __tests__/offlineSyncOutbox.test.js` | Passed | Confirmed retry metadata reset fields, unknown-table outbox-only terminalization, in-flight reset, missing-payload classification, and corresponding regression tests are present. |
| 2026-05-21 | `git diff --check` | Passed | No whitespace errors after follow-up Plan 4 review-agent fixes. |
| 2026-05-21 | Follow-up Plan 4 correctness review pass | Passed | Reviewer confirmed no blockers after unknown-table terminalization and retry metadata reset fixes; original critical flows remained intact. |
| 2026-05-21 | `npm test -- --runInBand __tests__/supabaseRequestQueue.test.js __tests__/AuthContext.test.js` | Failed as expected, then passed | Red tests reproduced missing Supabase request queue and stale post-signout profile update; green covers serialized queue execution, deferred auth profile reads, and stale-result suppression. |
| 2026-05-21 | `npm test -- --runInBand __tests__/ChildrenContext.test.js` | Failed as expected, then passed | Red failed on missing bundled preload service; green covers one preloaded child-data pull and cache preservation on partial pull errors. |
| 2026-05-21 | `npm test -- --runInBand __tests__/ClassesContext.plan5.test.js` | Failed as expected, then passed | Red caught missing active academic year on class creation and class delete double-writing children; green covers both Plan 5 contracts. |
| 2026-05-21 | `npm test -- --runInBand __tests__/AuthContext.test.js __tests__/supabaseRequestQueue.test.js __tests__/ChildrenContext.test.js __tests__/ClassesContext.plan5.test.js __tests__/ClassesContext.test.js` | Passed | Plan 5 auth/context targeted set passed 5 suites / 12 tests. Existing AuthContext console logs remain expected test noise. |
| 2026-05-21 | `rg "storage\\." src/screens src/hooks src/components` | Passed with migration callers | Captured the screen/hook/component storage caller list in this log before Task 3 edits, per Plan 5 Step 0. |
| 2026-05-21 | `npm test -- --runInBand` | Passed | Full Jest suite passed 37 suites / 174 tests after initial Plan 5 auth and context migration slices. Existing React Native Paper icon warnings, AuthContext logs, and OfflineContext logs remain non-blocking test noise. |
| 2026-05-21 | `npm test -- --runInBand __tests__/useTimeTracking.plan5.test.js` | Failed as expected, then passed | Red caught `useTimeTracking` still reading/writing through the storage facade; green switches clock-in, clock-out, and auto-clock-out to `timeEntriesRepository` and triggers background sync after local writes. |
| 2026-05-21 | `npm test -- --runInBand __tests__/TimeEntriesListScreen.plan5.test.js` | Failed as expected, then passed | Red caught Work History still doing a screen-owned Supabase pull and storage merge; green reads completed time entries from local SQLite via `timeEntriesRepository`. |
| 2026-05-21 | `npm test -- --runInBand __tests__/TimeEntriesListScreen.plan5.test.js __tests__/useTimeTracking.plan5.test.js` | Passed | Plan 5 time-tracking targeted set passed 2 suites / 2 tests. React Native Paper icon warnings remain expected non-blocking test noise. |
| 2026-05-21 | `npm test -- --runInBand __tests__/TimeEntriesListScreen.plan5.test.js __tests__/useTimeTracking.plan5.test.js __tests__/AuthContext.test.js __tests__/ChildrenContext.test.js __tests__/ClassesContext.plan5.test.js` | Passed | Plan 5 combined context/time targeted set passed 5 suites / 8 tests. Existing AuthContext logs and icon warnings remain expected non-blocking test noise. |
| 2026-05-21 | `npm test -- --runInBand` | Passed | Full Jest suite passed 39 suites / 176 tests after the Plan 5 time-tracking screen migration slice. Existing React Native Paper icon warnings, AuthContext logs, and OfflineContext logs remain non-blocking test noise. |
| 2026-05-21 | `git diff --check` | Passed | No whitespace errors after the Plan 5 time-tracking slice. |
| 2026-05-21 | `npm test -- --runInBand __tests__/SessionHistoryScreen.plan5.test.js` | Failed as expected, then passed | Red caught `SessionHistoryScreen` still doing a direct Supabase pull and storage merge; green reads recent session history from `sessionsRepository`. |
| 2026-05-21 | `npm test -- --runInBand __tests__/literacySessionPersistence.test.js` | Failed as expected, then passed | Red caught the missing transaction-backed persistence boundary; green saves sessions, attendees, and tracker changes through one SQLite transaction and rolls back the parent session if later tracker persistence fails. |
| 2026-05-21 | `npm test -- --runInBand __tests__/LetterTrackerBottomSheet.plan5.test.js` | Failed as expected, then passed | Red caught letter-tracker counts still using the storage facade; green reads assessments and letter mastery from repositories. |
| 2026-05-21 | `rg "storage\\." src/screens/sessions src/components/session -n` | Passed | No session-area screen/component storage facade calls remain after the session-history, session-save, and letter-tracker migration slice. |
| 2026-05-21 | `npm test -- --runInBand __tests__/SessionHistoryScreen.plan5.test.js __tests__/literacySessionPersistence.test.js __tests__/LetterTrackerBottomSheet.plan5.test.js __tests__/sessionsRepository.test.js __tests__/masteryRepository.test.js __tests__/TimeEntriesListScreen.plan5.test.js __tests__/useTimeTracking.plan5.test.js` | Passed | Combined Plan 5 session/time targeted set passed 7 suites / 10 tests. Existing icon warnings remain expected non-blocking test noise. |
| 2026-05-21 | `npm test -- --runInBand` | Passed | Full Jest suite passed 42 suites / 180 tests after the Plan 5 session-area migration slice. Existing React Native Paper icon warnings, AuthContext logs, and OfflineContext logs remain non-blocking test noise. |
| 2026-05-21 | `git diff --check` | Passed | No whitespace errors after the Plan 5 session-area migration slice. |
| 2026-05-21 | `npm test -- --runInBand __tests__/AssessmentHistoryScreen.plan5.test.js` | Failed as expected, then passed | Red caught assessment history still doing direct Supabase/storage merge; green reads recent assessment history from `assessmentsRepository`. |
| 2026-05-21 | `npm test -- --runInBand __tests__/LetterAssessmentScreen.plan5.test.js` | Failed as expected, then passed | Red caught `LetterAssessmentScreen` still saving through the storage facade; green saves assessments through `assessmentsRepository`, refreshes sync status, and triggers background sync. |
| 2026-05-21 | `rg "storage\\." src/screens src/hooks src/components -n` | Passed | No screen, hook, or component storage-facade calls remain after the assessment/dashboard/ranking migration. |
| 2026-05-21 | `npm test -- --runInBand __tests__/AssessmentHistoryScreen.plan5.test.js __tests__/LetterAssessmentScreen.plan5.test.js __tests__/LetterTrackerBottomSheet.plan5.test.js __tests__/literacySessionPersistence.test.js __tests__/SessionHistoryScreen.plan5.test.js __tests__/TimeEntriesListScreen.plan5.test.js __tests__/useTimeTracking.plan5.test.js __tests__/assessmentsRepository.test.js __tests__/sessionsRepository.test.js __tests__/masteryRepository.test.js` | Passed | Combined Plan 5 screen/repository targeted set passed 10 suites / 13 tests. Existing icon warnings remain expected non-blocking test noise. |
| 2026-05-21 | `npm test -- --runInBand` | Passed | Full Jest suite passed 44 suites / 182 tests after screen/hook/component storage callers were migrated to repositories. Existing React Native Paper icon warnings, AuthContext logs, and OfflineContext logs remain non-blocking test noise. |
| 2026-05-21 | `git diff --check` | Passed | No whitespace errors after the assessment/dashboard/ranking repository migration slice. |
| 2026-05-21 | `rg "storage\\.(getItem|setItem|removeItem)|STORAGE_KEYS" src __tests__ --glob '!src/utils/storage.js'` | Follow-up callers remain | Task 4 is not complete yet: remaining hits are context cache merge paths, asyncStorageSanitizer, and tests that explicitly verify old storage calls are not used. |
| 2026-05-21 | `rg "AsyncStorage" src --glob '!src/services/supabaseClient.js' --glob '!src/utils/logger.js' --glob '!src/utils/debugExport.js'` | Follow-up callers remain | Direct AsyncStorage is isolated to `src/utils/storage.js` plus a stale ChildrenContext comment; Task 4 cleanup remains for context/storage facade removal. |
| 2026-05-21 | `rg "storage\\.(getItem|setItem|removeItem)|STORAGE_KEYS" src __tests__ --glob '!src/utils/storage.js'` | Passed | No generic storage get/set/remove or `STORAGE_KEYS` callers remain outside `src/utils/storage.js` after context cache writes and tests were cleaned up. |
| 2026-05-21 | `rg "storage\\." src/screens src/hooks src/components -n` | Passed | No screen, hook, or component storage-facade calls remain after Task 4 cleanup. |
| 2026-05-21 | `rg "asyncStorageSanitizer|SchemaHardeningBootstrap|getSanitizerState|saveSanitizerState" src __tests__ -n` | Passed | Legacy AsyncStorage sanitizer, bootstrap component, tests, and facade state methods were removed for the clean-slate refactor. |
| 2026-05-21 | `rg "AsyncStorage" src --glob '!src/services/supabaseClient.js' --glob '!src/utils/logger.js' --glob '!src/utils/debugExport.js'` | Deferred residue | Remaining direct AsyncStorage references are centralized in `src/utils/storage.js` for the temporary storage/profile facade. Full facade removal is Plan 6 cleanup, not part of this Plan 5 slice. |
| 2026-05-21 | `npm test -- --runInBand __tests__/ChildrenContext.test.js __tests__/ClassesContext.plan5.test.js __tests__/ChildrenContext.hiddenChildren.test.js __tests__/storage-classes.test.js __tests__/AssessmentHistoryScreen.plan5.test.js __tests__/LetterAssessmentScreen.plan5.test.js` | Passed | Targeted Plan 5 cleanup suite passed 6 suites / 28 tests after context cache writes and sanitizer removal. |
| 2026-05-21 | `npm test -- --runInBand` | Passed | Full Jest suite passed 43 suites / 179 tests after Task 4 generic storage cleanup. Existing React Native Paper icon warnings, AuthContext logs, and OfflineContext logs remain non-blocking test noise. |
| 2026-05-21 | `git diff --check` | Passed | No whitespace errors after Task 4 generic storage cleanup. |
| 2026-05-21 | `npm test -- --runInBand __tests__/App.plan5.test.js` | Failed as expected, then passed | Red caught `App.js` still importing deleted `SchemaHardeningBootstrap`; green removed the stale wrapper and covers App-root render with lightweight providers. |
| 2026-05-21 | `npm run sqlite:staging:android` plus `adb reverse tcp:8082 tcp:8082` and Metro reload | Partial pass | Android Expo Go initially failed remote update, then bundled successfully after localhost reverse/reload and reached the Masi sign-in screen against `masi-app-sqlite`. Full signed-in flow remains blocked until a staging test user is available. Known Expo package-version warnings and signed-out Supabase auth lock warnings remain non-blocking. |
| 2026-05-21 | `npm test -- --runInBand` | Passed | Full Jest suite passed 44 suites / 180 tests after the App-root import fix and smoke test. Existing React Native Paper icon warnings, AuthContext logs, and OfflineContext logs remain non-blocking test noise. |
| 2026-05-21 | `git diff --check` | Passed | No whitespace errors after the App-root import fix and Android gate follow-up. |
| 2026-05-21 | `npm test -- --runInBand __tests__/ChildrenContext.test.js` | Failed as expected, then passed | Red caught that ChildrenContext still used unscoped child/group reads and legacy child+staff assignment creation; green uses `getMyChildren`, programme-scoped groups, and atomic `storage.createChild`. |
| 2026-05-21 | `npm test -- --runInBand __tests__/sessionsRepository.test.js __tests__/assessmentsRepository.test.js __tests__/masteryRepository.test.js __tests__/groupsRepository.test.js` | Failed as expected, then passed | Red caught legacy programme fallback and unscoped default reads; green removes write fallback and adds active-programme read scoping. |
| 2026-05-21 | `npm test -- --runInBand __tests__/classesRepository.test.js` | Failed as expected, then passed | Red caught user-scoped class reads returning classes from another programme; green filters active class assignments by user and active programme. |
| 2026-05-21 | `npm test -- --runInBand __tests__/literacySessionPersistence.test.js` | Failed as expected, then passed | Red caught tracker persistence reactivating a deleted mastery row from another programme; green resolves the session programme and scopes mastery lookups by programme. |
| 2026-05-21 | `npm test -- --runInBand __tests__/LetterTrackerBottomSheet.plan5.test.js` | Failed as expected, then passed | Red caught tracker count reads without user/child programme scope; green passes `userId` and `childId` to assessment and mastery repositories. |
| 2026-05-21 | `npm test -- --runInBand __tests__/AuthContext.test.js __tests__/ChildrenContext.test.js __tests__/ChildrenContext.hiddenChildren.test.js __tests__/classesRepository.test.js __tests__/childrenRepository.test.js __tests__/groupsRepository.test.js __tests__/sessionsRepository.test.js __tests__/assessmentsRepository.test.js __tests__/masteryRepository.test.js __tests__/literacySessionPersistence.test.js __tests__/LetterTrackerBottomSheet.plan5.test.js` | Passed | Corrective Plan 5 review-fix targeted suite passed 11 suites / 56 tests. Existing AuthContext console logs remain expected test noise. |
| 2026-05-21 | `npm test -- --runInBand` | Passed | Full Jest suite passed 44 suites / 197 tests after programme scoping, delete/archive, stale-auth, removed-membership, class archive, and child-create fixes. Existing React Native Paper icon warnings, AuthContext logs, and OfflineContext logs remain non-blocking test noise. |
| 2026-05-21 | `git diff --check` | Passed | No whitespace errors after Plan 5 corrective review fixes. |
| 2026-05-21 | `npm test -- --runInBand __tests__/AuthContext.test.js __tests__/supabaseClient.test.js __tests__/ClassesContext.plan5.test.js` | Failed as expected, then passed | Red caught duplicate auth startup/session initialization and non-singleton Supabase client behavior; green covers `INITIAL_SESSION`-only startup and one client/AppState listener across module reloads. |
| 2026-05-21 | `npm install @supabase/supabase-js@2.100.1` | Passed | Aligned Masi to Zazi's Supabase JS/Auth client version. npm reported the existing vulnerability count; no audit fix was run because package-audit remediation is outside this refactor slice. |
| 2026-05-21 | `npm run sqlite:staging:android` plus Android logcat | Passed | After restarting Metro outside the sandbox, Android reached the signed-in Home screen against `masi-app-sqlite`; fresh logcat cleared and observed for 35 seconds produced no Supabase auth lock warnings or React Native errors. Existing Expo package-version compatibility warnings remain non-blocking. |
| 2026-05-21 | `npm test -- --runInBand` | Passed | Full Jest suite passed 44 suites / 199 tests after Supabase client alignment and Android auth-lock fixes. Existing React Native Paper icon warnings, AuthContext logs, and OfflineContext logs remain non-blocking test noise. |
| 2026-05-21 | `git diff --check` | Passed | No whitespace errors after Supabase client alignment, auth startup, queued lookup/school reads, plan, and log updates. |
