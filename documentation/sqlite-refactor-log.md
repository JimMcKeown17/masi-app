# SQLite Refactor Log

This log is the durable record for the clean-slate AsyncStorage-to-SQLite refactor.

## Current Phase Checklist

- [ ] Spec signoff
- [ ] Plan 1 - Backend and config guardrails
- [ ] Plan 2 - Foundation and schema
- [ ] Plan 3 - Repositories and storage facade
- [ ] Plan 4 - Sync engine and outbox
- [ ] Plan 5 - Context and screen migration
- [ ] Plan 6 - Cleanup, export, and release gate

## Decision Register

| Date | Decision | Rationale | Revisit Trigger |
| --- | --- | --- | --- |
| 2026-05-20 | Use a clean-slate SQLite cutover instead of migrating local AsyncStorage domain data. | Field users can install a fresh app and restart from the new backend, which removes the riskiest compatibility work. | User decides current field-test device data must be preserved. |
| 2026-05-20 | Split the old monolithic plan into one spec plus six smaller implementation plans. | Zazi's refactor found many late review issues; smaller reviewable plans reduce conflict and make logs more useful. | A plan grows across unrelated ownership boundaries. |
| 2026-05-20 | Keep npm for this refactor. | The repo currently uses npm and package-lock; changing package managers during storage replacement adds avoidable risk. | User explicitly asks for pnpm migration or dependency security work becomes the main task. |
| 2026-05-20 | Separate job titles from programmes. | Job titles are profile/HR labels; programmes are operational dimensions for groups, sessions, assessments, and child work. | A staff member can hold multiple active programme assignments at the same time. |
| 2026-05-20 | Use `child_ea_assignments` and `child_group_memberships` in the clean schema. | The new backend is not constrained by old table names, and the EA wording matches the operational handoff model better than generic staff wording. | Masi decides non-EA staff roles must use the same table name. |
| 2026-05-20 | Migrate `app.json` to `app.config.js` in Plan 1. | Supabase target guardrails need dynamic config; treating this as optional leaves the old EAS `.env` pitfall in place. | Expo config needs to stay static for an EAS constraint we uncover. |
| 2026-05-20 | Auto-enroll newly created children into the actor's current programme. | Child creation should produce a complete working record without requiring a separate programme enrollment step. | EAs are allowed to create children for a programme other than their active assignment. |
| 2026-05-20 | Allow children to have multiple concurrent programme enrollments. | The same child may receive literacy, numeracy, and other support from different EAs; programme belongs to the work relationship, not permanently to the child. | Masi decides a child can belong to only one programme at a time. |
| 2026-05-20 | Scope My Children to the user's active programme. | EAs work one programme at a time, so the working list should show children assigned to the user and enrolled in that current programme. | Product needs a default cross-programme child list. |
| 2026-05-20 | Permit broad cross-programme reads for the same child at RLS level and enforce programme display in repositories. | Trusted field staff may need handover/history visibility, and simpler RLS is easier to debug than programme-isolated policies. | Masi adds less-trusted partner, parent, or external read-only users. |
| 2026-05-20 | Use dual SELECT policies for mobile-created tables. | Assignment/membership SELECT handles normal visibility; `created_by = auth.uid()` preserves upsert visibility before related join rows sync. | Backend no longer uses Supabase upserts from the mobile client. |
| 2026-05-20 | Enforce handover write blocking through active `child_ea_assignments`. | Historical assignees can read prior work, but old EAs should not keep writing child-specific event rows after handover. | Product wants old EAs to keep contributing after handover. |
| 2026-05-20 | Keep `time_entries` self-scoped by `user_id`. | Time tracking belongs to the staff member, not to child assignment state. | Time entries become manager-editable through the mobile app. |
| 2026-05-20 | Use one-shot field cutover with user-managed 1-2 day notice. | The user accepts losing unsynced device data at cutover and prefers a clean simultaneous restart. | Field operations require a rolling or reversible cutover. |
| 2026-05-20 | Keep `AGENTS.md` and `CLAUDE.md` aligned with the clean-slate refactor before implementation starts. | Future agents should not optimize for legacy backwards compatibility or skip the refactor log/TDD discipline after the user locked the clean-slate decision. | The refactor finishes or the user changes the cutover strategy. |
| 2026-05-21 | Treat `masi-app-sqlite` as the clean-slate backend intended to become primary after validation. | The user created the new Supabase project and left current `masi-app` intact, matching the clean-slate refactor plan. | User decides to keep `masi-app-sqlite` staging-only and create a separate production backend later. |
| 2026-05-21 | Add `academic_years` as a first-class table; classes become year-specific via `academic_year_id`. | User confirmed multi-year operation; without academic years, year-over-year reporting collides on class name. | Masi pivots to single-year operation. |
| 2026-05-21 | Use a hard-fail invariant for multiple active academic years. | The partial unique index keeps year activation deliberate and simple; the active 2026 seed supplies the required current year. | Admin workflow needs one migration to activate a new year and deactivate the old one atomically. |
| 2026-05-21 | Add `assessment_windows` with baseline/midline/endline; tie official assessments to a window and leave ad-hoc progress checks windowless. | Formal assessment campaigns are part of Masi reporting; date-range inference is too brittle. | Masi drops formal assessment windows. |
| 2026-05-21 | Add `child_class_memberships` with academic year; `children.class_id` becomes a current/compatibility pointer. | Children can change classes mid-year, and old work must stay linked to the old class for reports. | Masi decides children never change classes mid-year. |
| 2026-05-21 | Add `teachers` table without teacher assignment history tables. | Reporting on teachers is not planned now but might come later; a first-class current teacher table is cheap to add now and expensive to retrofit. | Reporting needs teacher-level history; add `teacher_school_assignments` and `class_teacher_assignments` then. |
| 2026-05-21 | Add `class_ea_assignments` and `group_ea_assignments`; EAs can be assigned at child, class, or group level. | User confirmed Masi uses a mix of all three assignment levels in practice. | Masi simplifies to a single assignment level. |
| 2026-05-21 | Add `grouping_versions` and `class_grouping_state` for versioned grouping. | EAs regroup mid-year and prior groupings must remain as history. | Masi decides regrouping is rare enough to do in place without versioning. |
| 2026-05-21 | Skip Zazi tables `education_assistants`, `child_school_enrollments`, `staff_identity_links`, external ID bridges, `teacher_school_assignments`, and `class_teacher_assignments`. | Each can be added later if the operational model needs it; carrying them now adds RLS surface without clear value. | Admin/PM features need a separate EA roster, child mobility between schools becomes common, or teacher history becomes reportable. |
| 2026-05-21 | Write policy for child-specific event rows uses an OR across direct child assignment, class assignment plus active class membership, and group assignment plus active group membership. | EAs can earn write access through any of the three assignment paths; all paths must include both active assignment and active membership filters to prevent stale-assignment writes. | A new assignment path is added, or the multi-path OR becomes too expensive per row. |
| 2026-05-21 | Disable direct mobile hard delete on `children`; use `delete_child_if_no_history(child_id)` for the rare no-history delete path. | Child event rows have cascading FKs, so direct delete could silently erase assessment, mastery, and attendance history. Children with history should be archived or unassigned instead. | Product introduces a server-side admin console with explicit audited delete approval. |
| 2026-05-21 | Archive repositories must end active assignments/enrollments in the same transaction as the archive marker. | Archived children/classes/groups should not stay active in My Children, class, group, or assignment reports. | Archive semantics change to mean display-only hidden state. |
| 2026-05-21 | Admin-precreated classes are supported; EAs may self-assign only when the class is visible through their active school/programme assignment. | Masi will have admin-created/prepopulated classes; requiring `classes.created_by = auth.uid()` would block legitimate mobile claiming. | Admin workflows move all class assignment through service-role only. |
| 2026-05-21 | Future active-year rollover is a deliberate migration transaction, not automatic year switching. | The active-year unique index hard-fails multiple active years; future maintainers need to deactivate the old year and activate the new year atomically. | Masi wants an admin UI to change active years interactively. |

## Bug And Gap Register

| Date | Gap/Bug | Resolution | Reuse Note |
| --- | --- | --- | --- |
| 2026-05-20 | Original plan over-weighted backwards compatibility. | Superseded with clean-slate spec and six smaller plans. | Ask whether field data must be preserved before designing storage migrations. |
| 2026-05-20 | Zazi review findings were present as lessons but not mandatory contracts. | Added transaction, outbox finalization, pull concurrency, PRAGMA, RLS, and real-device validation contracts to the sub-plans. | Every Zazi production trap should become a testable contract. |
| 2026-05-20 | `AGENTS.md` still emphasized backwards-compatible production schema changes without calling out the SQLite refactor exception. | Updated `AGENTS.md` and `CLAUDE.md` to point to the clean-slate spec, TDD skill, npm exception, app.config.js migration, and mandatory refactor log. | Keep agent context files aligned when major project direction changes. |
| 2026-05-21 | Shell-sourcing `.env.local` failed because `SUPABASE_DB_PASSWORD_SQLITE` contains shell metacharacters. | Plan 1 helper must parse `.env` / `.env.local` as dotenv-style data and must not execute the file with `.` or `source`. | Any future script reads Supabase DB passwords from env files. |
| 2026-05-21 | Fresh `masi-app-sqlite` advisors report `public.rls_auto_enable()` as a SECURITY DEFINER function executable by `anon` and `authenticated`. | Resolved in `20260521120147_masi_rls_advisor_cleanup.sql` by revoking execute from `public`, `anon`, and `authenticated`. | A new Supabase project ships with exposed helper functions. |
| 2026-05-21 | After the first Plan 1 migrations were applied, advisors still reported `rls_auto_enable()` execute exposure and many `auth_rls_initplan` policy warnings. | Resolved with a dedicated RLS advisor cleanup migration that uses `(select auth.uid())` in policy predicates. Dual SELECT `multiple_permissive_policies` warnings on `children`, `classes`, and `groups` remain intentional because they preserve mobile upsert visibility. | Any future RLS migration uses `(select auth.uid())` and explicitly handles security-definer function execute privileges. |
| 2026-05-21 | `sqlite:staging:push` prompted for confirmation during the cleanup migration push. | Added `--yes` to the helper's `db-push` action so future migration pushes are non-interactive. | Any CLI helper command is used in a release gate or CI-like flow. |
| 2026-05-21 | Self-review found `staff_programme_assignments` did not enforce the one-active-programme-per-EA assumption. | Add a partial unique index on `staff_programme_assignments(user_id)` where `ended_at is null`. | Product allows one EA to actively work multiple programmes at the same time. |
| 2026-05-21 | Plan 1 review found recursive RLS between `sessions` SELECT and `session_attendees` SELECT, producing `42P17 infinite recursion detected`. | Add non-recursive private helper functions and replace the session-related SELECT policies. | Any RLS policy on one table queries another table whose policy queries back into the first table. |
| 2026-05-21 | Plan 1 review found staging helper guardrails could target the wrong Supabase project if `_SQLITE` env vars are misconfigured. | Require the known `masi-app-sqlite` project ref and exact Supabase hostname in staging validation. | New SQLite backend project ref changes intentionally. |
| 2026-05-21 | Plan 1 review found session parent write policies did not enforce the handover contract after attendees exist. | Tighten session update/delete policies to require an active assignment for at least one session attendee; keep insert parent programme-scoped because attendees are inserted after the parent row and enforce child assignment. | Session creation moves to a backend RPC that can validate parent and attendees atomically. |
| 2026-05-21 | Plan 1 review found `assessments` and `letter_mastery` UPDATE checks did not re-check the new child/programme state. | Mirror insert-style active child assignment and active programme checks in `WITH CHECK`. | Repository-level immutability is later proven sufficient and RLS can be simplified safely. |
| 2026-05-21 | Follow-up Plan 1 review found `child_ea_assignments` could grant child access too broadly and attendee update/delete did not require parent session ownership. | Add a corrective migration that limits mobile assignment insert to children the actor created, removes mobile assignment update/delete policies, and routes attendee update/delete through a private ownership helper. | Masi adds a server-controlled handover RPC or admin mobile flow. |
| 2026-05-21 | Follow-up Plan 1 review found `sqlite:staging:link` passed the DB password as a process argument. | Move link to `supabase --yes link --project-ref ...` and rely on `SUPABASE_DB_PASSWORD` in the command environment if supported by the CLI. | Supabase CLI requires explicit `--password`; document the process-arg limitation if fallback is needed. |
| 2026-05-21 | Initial Masi clean-slate schema was leaner than Zazi by eight tables. | Add a Zazi-alignment migration with academic years, assessment windows, teachers, child-class history, class/group EA assignments, grouping versions, class grouping state, column additions, and an updated RLS helper. | Compare new clean-slate schemas against the most-similar existing app before treating a spec as final. |
| 2026-05-21 | Seed migration did not include schools, an active academic year, or a baseline assessment window. | Extend Plan 1 with Zazi-alignment seed data: active 2026 year, 2026 baseline window, and 325 schools from `scripts/masi-schools-db-apr26.csv`. | Treat "first sign-in shows useful reference data" as a Plan 1 acceptance criterion. |
| 2026-05-21 | Review found direct child hard delete would cascade and erase child history. | Add a child delete guard migration that drops the child delete policy, revokes direct delete, and exposes only `delete_child_if_no_history(child_id)` for no-history deletes. | Any new table references `children(id)` with `on delete cascade`; update the RPC's history check before using hard delete. |

## Verification Register

| Date | Command/Check | Result | Notes |
| --- | --- | --- | --- |
| 2026-05-20 | `rg -n "clean-slate|sqlite-refactor-log|Test Driven Development|backwards-compatible|app.config.js|TDD skill|/Users/jimmckeown/Development/masi-app/.agents/skills/tdd" AGENTS.md CLAUDE.md` | Passed | Confirmed both agent context files mention the refactor, log, TDD skill, compatibility exception, and `app.config.js` direction. |
| 2026-05-20 | `rg -n "[[:blank:]]$" AGENTS.md CLAUDE.md docs/superpowers/specs/2026-05-20-sqlite-migration-design.md docs/superpowers/plans/2026-05-20-sqlite-*.md documentation/sqlite-refactor-log.md` | Passed | No trailing whitespace in updated docs. |
| 2026-05-21 | `supabase --version` | Passed | CLI version `2.95.4`. |
| 2026-05-21 | `supabase projects list -o json` | Passed | CLI authenticated; existing `masi-app` and new `masi-app-sqlite` are visible. New project ref is `segygjzpujphwvrubusm` and status is `ACTIVE_HEALTHY`. |
| 2026-05-21 | `supabase link --project-ref segygjzpujphwvrubusm --password ... --yes` | Passed | Repo is linked to the new `masi-app-sqlite` project for Plan 1. Secret was read from `.env.local` and not printed. |
| 2026-05-21 | `cat supabase/.temp/project-ref` | Passed | Linked project ref is `segygjzpujphwvrubusm`; `supabase/.temp/` is gitignored. |
| 2026-05-21 | `supabase db query --linked "select current_database() as database_name, current_user as database_user" -o json` | Passed | Query reached the linked remote database and returned `postgres` / `postgres`. |
| 2026-05-21 | `SUPABASE_DB_PASSWORD=... supabase migration list --linked` | Passed | New project has no local or remote migrations yet. |
| 2026-05-21 | `SUPABASE_DB_PASSWORD=... supabase db push --linked --dry-run` | Passed | Remote database is up to date; dry run did not attempt to push changes. |
| 2026-05-21 | `SUPABASE_DB_PASSWORD=... supabase db advisors --linked --level warn` | Warning | Reported `public.rls_auto_enable()` SECURITY DEFINER function executable by `anon` and `authenticated`; captured as a Plan 1 security item. |
| 2026-05-21 | `npm test -- --runInBand __tests__/supabaseProjectConfig.test.js` | Passed | TDD slice for `config/supabaseProjectConfig.js` and `app.config.js`; first run failed on missing modules, then passed after resolver/config implementation. |
| 2026-05-21 | `npm test -- --runInBand __tests__/supabaseClient.test.js __tests__/supabaseProjectConfig.test.js` | Passed | Supabase client now refuses mismatched target/project URL combinations before creating a client. |
| 2026-05-21 | `npm test -- --runInBand __tests__/sqlitePlan1Migrations.test.js` | Failed as expected | Red TDD check: no `rls_advisor_cleanup` migration exists yet to revoke PUBLIC execute or convert policy calls to `(select auth.uid())`. |
| 2026-05-21 | `npm test -- --runInBand __tests__/sqliteStagingScript.test.js` | Failed as expected | Red TDD check: `db-push` did not include `--yes`, so the helper could prompt interactively. |
| 2026-05-21 | `npm test -- --runInBand __tests__/sqlitePlan1Migrations.test.js` | Passed | Cleanup migration now revokes PUBLIC execute and every `auth.uid()` in that migration is wrapped as `(select auth.uid())`. |
| 2026-05-21 | `npm run sqlite:staging:dry-run` | Passed | Would push only `20260521120147_masi_rls_advisor_cleanup.sql`. First sandboxed attempt failed with DNS resolution, then passed with network access. |
| 2026-05-21 | `npm run sqlite:staging:push` | Passed | Applied `20260521120147_masi_rls_advisor_cleanup.sql` to `masi-app-sqlite`. This run revealed the interactive prompt captured in the bug register. |
| 2026-05-21 | `npm test -- --runInBand __tests__/sqliteStagingScript.test.js` | Passed | Staging helper now includes `--yes` for `db-push` and still redacts secrets. |
| 2026-05-21 | `npm run sqlite:staging:migrations` | Passed | Local and remote migration history match: `20260521115412`, `20260521115416`, `20260521115421`, `20260521120147`. |
| 2026-05-21 | `npm run sqlite:staging:advisors` | Expected warnings only | Remaining warnings are `multiple_permissive_policies` for `public.children`, `public.classes`, and `public.groups` SELECT policies. These are intentional dual SELECT policies for assignment/programme visibility plus `created_by` upsert fallback. |
| 2026-05-21 | `npm test -- --runInBand __tests__/supabaseProjectConfig.test.js __tests__/supabaseClient.test.js __tests__/sqliteStagingScript.test.js __tests__/sqlitePlan1Migrations.test.js` | Passed | Plan 1 targeted test set passed 4 suites / 13 tests. |
| 2026-05-21 | `npm run sqlite:staging:check` | Passed | SQLite staging env loads from `.env.local`, validates project ref/URL, and redacts DB password and publishable key. |
| 2026-05-21 | `rg -n "replace\\(|pg_policies|staff_children|children_groups|children_ids|correct_letters|incorrect_letters" supabase/migrations` | Passed | No forbidden policy string replacement or legacy table/JSON field names in canonical Supabase migrations. |
| 2026-05-21 | `npm test -- --runInBand` | Passed | Full Jest suite passed 19 suites / 81 tests. Existing React Native Paper icon warnings remain non-blocking test noise. |
| 2026-05-21 | `npm run sqlite:staging:dry-run` | Passed | Remote database is up to date after the cleanup migration. |
| 2026-05-21 | `npm test -- --runInBand __tests__/sqlitePlan1Migrations.test.js` | Failed as expected | Red TDD check: no partial unique index enforced one active `staff_programme_assignments` row per user. |
| 2026-05-21 | `npm test -- --runInBand __tests__/sqlitePlan1Migrations.test.js` | Passed | Added `20260521135520_masi_staff_programme_assignment_uniqueness.sql` with a partial unique index on active staff programme assignments. |
| 2026-05-21 | `npm run sqlite:staging:dry-run` | Passed | Would push only `20260521135520_masi_staff_programme_assignment_uniqueness.sql`. |
| 2026-05-21 | `npm run sqlite:staging:push` | Passed | Applied `20260521135520_masi_staff_programme_assignment_uniqueness.sql` to `masi-app-sqlite`. |
| 2026-05-21 | `npm run sqlite:staging:migrations` | Passed | Local and remote migration history now match through `20260521135520`. |
| 2026-05-21 | `npm run sqlite:staging:advisors` | Expected warnings only | Still only the intentional `multiple_permissive_policies` warnings for `children`, `classes`, and `groups`. |
| 2026-05-21 | `npm run sqlite:staging:dry-run` | Passed | Remote database is up to date after the staff assignment uniqueness migration. |
| 2026-05-21 | Plan 1 parallel review checkpoint | Failed | Review found RLS recursion, staging-project guardrail weakness, session handover write gaps, incomplete update `WITH CHECK` predicates, and substring URL matching. |
| 2026-05-21 | `npm test -- --runInBand __tests__/supabaseProjectConfig.test.js __tests__/sqliteStagingScript.test.js __tests__/sqlitePlan1Migrations.test.js` | Failed as expected | Red TDD check for review findings: URL substring guard accepted malformed hosts, staging helper accepted primary project env, and no `rls_review_fixes` migration existed yet. |
| 2026-05-21 | `npm test -- --runInBand __tests__/supabaseProjectConfig.test.js __tests__/sqliteStagingScript.test.js __tests__/sqlitePlan1Migrations.test.js` | Passed | Exact Supabase hostname validation, known SQLite project-ref validation, private RLS helpers, session update/delete handover checks, and assessment/letter mastery update `WITH CHECK` contracts are now covered. |
| 2026-05-21 | `npm test -- --runInBand __tests__/sqliteStagingScript.test.js` | Failed as expected | Red TDD check: Supabase CLI still prompted when `--yes` was placed after `db push`; move it to the global flag position. |
| 2026-05-21 | `npm test -- --runInBand __tests__/sqliteStagingScript.test.js` | Passed | `db-push` now calls Supabase as `supabase --yes db push --linked`. |
| 2026-05-21 | `npm run sqlite:staging:dry-run` | Passed | Would push only `20260521140331_masi_rls_review_fixes.sql`. |
| 2026-05-21 | `npm run sqlite:staging:push` | Passed | Applied `20260521140331_masi_rls_review_fixes.sql` to `masi-app-sqlite`. This run still printed the old prompt because it ran before the helper was changed to put `--yes` in the global flag position. |
| 2026-05-21 | `npm run sqlite:staging:migrations` | Passed | Local and remote migration history now match through `20260521140331`. |
| 2026-05-21 | `npm run sqlite:staging:advisors` | Expected warnings only | Still only the intentional `multiple_permissive_policies` warnings for `children`, `classes`, and `groups`; no security-definer or `auth_rls_initplan` warnings returned. |
| 2026-05-21 | Rollback-only RLS recursion probe through `supabase db query --linked` | Passed | As authenticated user `00000000-0000-0000-0000-000000000101`, session and attendee SELECTs each returned 1 row with no `42P17` recursion. Transaction rolled back. |
| 2026-05-21 | Rollback-only RLS handover update probe through `supabase db query --linked` | Probe harness failed first, then passed | First attempt could not insert into a temp results table after switching to `authenticated`; rerun granted temp-table access. Active assignment updated 1 session row; after `child_ea_assignments.unassigned_at`, the same authenticated update returned 0 rows. Transaction rolled back. |
| 2026-05-21 | `npm run sqlite:staging:check` | Passed | Stricter staging helper accepts only `segygjzpujphwvrubusm` and redacts secrets. |
| 2026-05-21 | `npx expo config --type public` | Passed | Dynamic `app.config.js` resolves the default primary target and preserves app identity, runtime policy, update URL, and EAS project ID. |
| 2026-05-21 | `npm test -- --runInBand __tests__/supabaseProjectConfig.test.js __tests__/supabaseClient.test.js __tests__/sqliteStagingScript.test.js __tests__/sqlitePlan1Migrations.test.js` | Passed | Targeted Plan 1 test set passed 4 suites / 18 tests after review fixes. |
| 2026-05-21 | `git diff --check` | Passed | No whitespace errors in the Plan 1 diff. |
| 2026-05-21 | `rg -n "replace\\(|pg_policies|staff_children|children_groups|children_ids|correct_letters|incorrect_letters" supabase/migrations` | Passed | No forbidden RLS string replacement and no legacy storage table/JSON names in canonical migrations. |
| 2026-05-21 | `npm test -- --runInBand` | Passed | Full Jest suite passed 19 suites / 86 tests. Existing React Native Paper icon warnings remain non-blocking test noise. |
| 2026-05-21 | Follow-up Plan 1 parallel review checkpoint | Failed | Review found assignment-table access escalation, attendee update/delete ownership gap, and DB password process-argument exposure in `sqlite:staging:link`. |
| 2026-05-21 | `npm test -- --runInBand __tests__/sqlitePlan1Migrations.test.js __tests__/sqliteStagingScript.test.js` | Failed as expected | Red TDD check for follow-up review findings: no `assignment_attendee_fixes` migration and link still included DB password in argv. |
| 2026-05-21 | `npm test -- --runInBand __tests__/sqlitePlan1Migrations.test.js __tests__/sqliteStagingScript.test.js` | Passed | Follow-up review fixes are now covered by migration and staging-helper tests. |
| 2026-05-21 | `npm run sqlite:staging:dry-run` | Passed | Would push only `20260521142324_masi_assignment_attendee_fixes.sql`. |
| 2026-05-21 | `npm run sqlite:staging:push` | Passed | Applied `20260521142324_masi_assignment_attendee_fixes.sql` to `masi-app-sqlite`. Supabase CLI still prints the confirmation prompt even with `--yes`, but proceeds without manual secret exposure. |
| 2026-05-21 | `npm run sqlite:staging:link` | Passed | Updated link helper works without passing the DB password in process args; password is supplied through `SUPABASE_DB_PASSWORD` env. |
| 2026-05-21 | `npm run sqlite:staging:migrations` | Passed | Local and remote migration history now match through `20260521142324`. |
| 2026-05-21 | `npm run sqlite:staging:advisors` | Expected warnings only | Still only the intentional `multiple_permissive_policies` warnings for `children`, `classes`, and `groups`. |
| 2026-05-21 | Rollback-only assignment escalation probe through `supabase db query --linked` | Probe harness failed | Shell expanded PL/pgSQL `$$` delimiters into a process ID, causing SQL syntax error before the RLS check ran. Rerun with escaped delimiters. |
| 2026-05-21 | Rollback-only assignment escalation probe through `supabase db query --linked` | Failed | Both own-child and other-child assignment inserts returned `42P17`, because the assignment INSERT policy queried `children`, whose SELECT policy queries `child_ea_assignments`. Add a private helper to bypass RLS recursion for this policy check. |
| 2026-05-21 | `npm test -- --runInBand __tests__/sqlitePlan1Migrations.test.js` | Failed as expected | Red TDD check: no `assignment_insert_recursion_fix` migration exists yet. |
| 2026-05-21 | `npm test -- --runInBand __tests__/sqlitePlan1Migrations.test.js` | Passed | Added `20260521143346_masi_assignment_insert_recursion_fix.sql` with private helper-backed assignment insert policy. |
| 2026-05-21 | `npm run sqlite:staging:dry-run` | Passed | Would push only `20260521143346_masi_assignment_insert_recursion_fix.sql`. |
| 2026-05-21 | `npm run sqlite:staging:push` | Passed | Applied `20260521143346_masi_assignment_insert_recursion_fix.sql` to `masi-app-sqlite`; Supabase CLI still displays its confirmation prompt while auto-answering yes. |
| 2026-05-21 | Rollback-only assignment escalation probe through `supabase db query --linked` | Passed | As authenticated User B, assignment to User B's own created child inserted; assignment to User A's child returned `42501`. Transaction rolled back. |
| 2026-05-21 | Rollback-only cross-EA attendee update probe through `supabase db query --linked` | Passed | Other active EA on the same child updated 0 attendee rows; session owner EA updated 1 attendee row. Transaction rolled back. |
| 2026-05-21 | `npm run sqlite:staging:migrations` | Passed | Local and remote migration history now match through `20260521143346`. |
| 2026-05-21 | `npm run sqlite:staging:advisors` | Expected warnings only | Still only the intentional `multiple_permissive_policies` warnings for `children`, `classes`, and `groups`. |
| 2026-05-21 | `npm run sqlite:staging:dry-run` | Passed | Remote database is up to date after all Plan 1 corrective migrations. |
| 2026-05-21 | `npm test -- --runInBand __tests__/supabaseProjectConfig.test.js __tests__/supabaseClient.test.js __tests__/sqliteStagingScript.test.js __tests__/sqlitePlan1Migrations.test.js` | Passed | Final targeted Plan 1 suite passed 4 suites / 21 tests. |
| 2026-05-21 | `npm run sqlite:staging:check` | Passed | Final staging env guard check accepts only `segygjzpujphwvrubusm` and redacts secrets. |
| 2026-05-21 | `git diff --check` | Passed | No whitespace errors in the final Plan 1 diff. |
| 2026-05-21 | `rg -n "replace\\(|pg_policies|staff_children|children_groups|children_ids|correct_letters|incorrect_letters" supabase/migrations` | Passed | No forbidden RLS string replacement and no legacy storage table/JSON names in canonical migrations. |
| 2026-05-21 | `npm test -- --runInBand` | Passed | Final full Jest suite passed 19 suites / 89 tests. Existing React Native Paper icon warnings remain non-blocking test noise. |
| 2026-05-21 | `npm test -- --runInBand __tests__/sqlitePlan1Migrations.test.js` | Failed as expected, then passed | Red TDD check failed because no Zazi-alignment migration existed; after adding `20260521144901_masi_zazi_alignment_schema.sql`, the migration contract suite passed 12 tests. |
| 2026-05-21 | `rg -n "replace\\(|pg_policies|staff_children|children_groups|children_ids|correct_letters|incorrect_letters|education_assistants|child_school_enrollments|staff_identity_links|teacher_school_assignments|class_teacher_assignments" supabase/migrations` | Passed | No forbidden policy string replacement, legacy storage names, or explicitly skipped Zazi tables in canonical migrations. |
| 2026-05-21 | `npm run sqlite:staging:dry-run` | Passed | Would push only `20260521144901_masi_zazi_alignment_schema.sql`. |
| 2026-05-21 | `npm run sqlite:staging:push` | Passed after sandbox retry | First sandboxed attempt failed DNS resolution; rerun with network access applied `20260521144901_masi_zazi_alignment_schema.sql` to `masi-app-sqlite`. Supabase CLI still printed the confirmation prompt while auto-answering yes. |
| 2026-05-21 | `npm run sqlite:staging:migrations` | Passed | Local and remote migration history now match through `20260521144901`. |
| 2026-05-21 | `npm run sqlite:staging:advisors` | Expected warnings only | Still only the intentional `multiple_permissive_policies` warnings for `children`, `classes`, and `groups`. |
| 2026-05-21 | `supabase db query --linked` reference seed probe | Passed | Remote `masi-app-sqlite` has one active 2026 academic year, one 2026 baseline assessment window, and 325 schools. Transaction-free read only. |
| 2026-05-21 | Rollback-only active-year invariant probe through `supabase db query --linked` | Passed | Inserting a second active academic year raised `unique_violation`; transaction rolled back. |
| 2026-05-21 | Rollback-only assessment window/purpose probe through `supabase db query --linked` | Passed | Inserting an `official_window` assessment with `assessment_window_id = null` raised `check_violation`; transaction rolled back. |
| 2026-05-21 | Rollback-only child write helper probe through `supabase db query --linked` | Passed | `private.current_user_can_write_for_child` returned true via direct child, class assignment, and group assignment paths, and false for no path, ended class membership, and ended group membership. Transaction rolled back. |
| 2026-05-21 | Rollback-only grouping-version uniqueness probe through `supabase db query --linked` | Passed | Inserting a second active `grouping_versions` row for one class/year raised `unique_violation`; transaction rolled back. |
| 2026-05-21 | Rollback-only baseline trigger probe through `supabase db query --linked` | Passed | Temporarily deactivating 2026 and inserting active 2098 created one baseline assessment window; transaction rolled back. |
| 2026-05-21 | `npm test -- --runInBand __tests__/supabaseProjectConfig.test.js __tests__/supabaseClient.test.js __tests__/sqliteStagingScript.test.js __tests__/sqlitePlan1Migrations.test.js` | Passed | Targeted Plan 1 suite passed 4 suites / 26 tests after Zazi-alignment amendments. |
| 2026-05-21 | `npm run sqlite:staging:dry-run` | Passed | Remote database is up to date after applying the Zazi-alignment migration. |
| 2026-05-21 | `git diff --check` | Passed | No whitespace errors after migration, spec, plan, and log updates. |
| 2026-05-21 | `npm test -- --runInBand` | Passed | Full Jest suite passed 19 suites / 94 tests. Existing React Native Paper icon warnings remain non-blocking test noise. |
| 2026-05-21 | `npm run sqlite:staging:check` | Passed | SQLite staging env still validates the `masi-app-sqlite` project ref/URL and redacts secrets. |
| 2026-05-21 | `npm test -- --runInBand __tests__/sqlitePlan1Migrations.test.js` | Failed as expected, then passed | Red TDD check failed because no child delete guard migration existed; after adding `20260521153217_masi_child_delete_guard.sql`, the migration contract suite passed 15 tests. |
| 2026-05-21 | `npm run sqlite:staging:dry-run` | Passed | Would push only `20260521153217_masi_child_delete_guard.sql`. |
| 2026-05-21 | `npm run sqlite:staging:push` | Passed after sandbox retry | First sandboxed attempt failed DNS resolution; rerun with network access applied the child delete guard migration to `masi-app-sqlite`. |
| 2026-05-21 | `npm run sqlite:staging:migrations` | Passed | Local and remote migration history now match through `20260521153217`. |
| 2026-05-21 | `npm run sqlite:staging:advisors` | Expected warnings only | Still only the intentional `multiple_permissive_policies` warnings for `children`, `classes`, and `groups`. |
| 2026-05-21 | Rollback-only child delete guard probe through `supabase db query --linked` | Passed | Direct authenticated child delete failed with `insufficient_privilege`; `delete_child_if_no_history` deleted a no-history child, returned false for a child with assessment history, and mobile self-assignment to an admin-created class succeeded. Transaction rolled back. |
| 2026-05-21 | `npm test -- --runInBand __tests__/supabaseProjectConfig.test.js __tests__/supabaseClient.test.js __tests__/sqliteStagingScript.test.js __tests__/sqlitePlan1Migrations.test.js` | Passed | Targeted Plan 1 suite passed 4 suites / 29 tests after child delete guard. |
| 2026-05-21 | `npm run sqlite:staging:dry-run` | Passed | Remote database is up to date after applying the child delete guard migration. |
| 2026-05-21 | `git diff --check` | Passed | No whitespace errors after child delete guard, archive-contract, admin-class, and active-year docs updates. |
| 2026-05-21 | `npm test -- --runInBand` | Passed | Full Jest suite passed 19 suites / 97 tests. Existing React Native Paper icon warnings remain non-blocking test noise. |
